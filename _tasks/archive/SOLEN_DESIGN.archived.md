# Solen Design System — Source of Truth

> **Status:** ACTIVE · **Last revised:** 2026-04-20 · **Preview:** `public/solen-coral.html` → `http://localhost:3000/solen-coral.html`

This is the **single** design doc for Solen. Ignore any other design file in this repo — if it contradicts this doc, this doc wins.

---

## 0. THE BRAND IN ONE SENTENCE

Solen is the **Swiss beauty marketplace for Basel** — warm, neighborhood-local, editorial. An app you use daily that feels like a Swiss independent magazine printed your haircut. Never corporate, never salesy, never cold grey.

**Voice examples (Swiss-wide, Q5 2026-04-22):** "Von der Schweiz. Für dich." · "Beauty, direkt gebucht." · "Dein Salon. Direkt buchen." · "Coiffeur, Barber, Nails & Spa — buche jetzt in deinem Quartier." · Dynamic: "Für Basel" / "Für Zürich" / "Für Bern" based on detected city; fallback "Für deine Stadt."

---

## 1. PALETTE (LOCKED)

Warm, confident, never cool. Built around **cream** base + **terracotta coral** accent + **warm ink** text.

### Core

```css
--bg:       #FFFFFF;   /* WHITE page base (Q15 2026-04-22) */
--sur:      #F5F0E8;   /* warm surface — recessed states, NOT page bg */
--raised:   #FFFFFF;   /* card surface */
--sun:      #EDE5D8;   /* warm recessed (inputs, keep for card depth) */

--ink:      #1A1209;   /* primary text — warm ink, never #000 */
--ink2:     #4A3D2E;   /* secondary text */
--ink3:     #8A7A66;   /* tertiary / meta */
--ink4:     #C4B8A6;   /* placeholder / disabled */
```

### Brand

```css
--coral:    #E8624A;   /* PRIMARY brand · CTAs · accents */
--coral-h:  #CC4E35;   /* hover */
--coral-s:  #FAECE7;   /* subtle bg */
--coral-t:  #7A2415;   /* WCAG-safe small text on cream */
```

### Extended family (use purposefully, not randomly)

```css
--amber:    #D4870A;  /* secondary CTA, eyebrow labels, coiffeur tint */
--amber-s:  #FEF4E0;  --amber-t: #6B4005;

--blue:     #6BA3C8;  /* Basel blue, map pins, farbe tag */
--blue-s:   #EAF3FB;  --blue-t: #1A4D72;

--plum:     #4A1E3C;  /* depth blocks (Last Minute section bg) */

--sage:     #7BA688;  /* spa/wellness, massage */
--sage-s:   #EBF5EE;  --sage-t: #2E5E3A;

--yellow:   #F2C144;  /* "Solen Top Pick" badges, highlight numerals */
--yellow-s: #FEF8E0;  --yellow-t: #7A5C00;

--sand:     #C9A96E;  /* warm surfaces, makeup tint */
```

### Borders

```css
--b:  rgba(26,18,9,.08);   /* hairline */
--b2: rgba(26,18,9,.15);   /* stronger */
--b3: rgba(26,18,9,.28);   /* outline buttons */
```

### BANNED

- ❌ Pure black `#000000` — use `--ink` (`#1A1209`)
- ❌ Cool greys, bluish greys — use warm ink opacity
- ❌ Fluorescent / saturated primaries outside this palette
- ❌ Green+peach (old V2 attempt) — retired
- ❌ Any hex value not declared here

### 60-30-10 rule
- 60% cream/ink base (`--bg` + `--ink`)
- 30% white/surface cards (`--raised` + `--sur`)
- 10% coral + extended family accents

---

## 2. TYPOGRAPHY (LOCKED)

Two families, strict hierarchy. Loaded from Google Fonts.

```css
@import url('https://fonts.googleapis.com/css2?family=Anton&family=Figtree:wght@400;500;600;700&display=swap');

--font-display: "Anton", Impact, sans-serif;       /* declarative editorial display — uppercase headlines */
--font-body:    "Figtree", system-ui, sans-serif;  /* everything else — body, eyebrows, buttons, meta, prices, forms */
```

### When to use which

| Font | Use for | Rules |
|------|---------|-------|
| **Anton** | Hero headline (Q48 signature lockup), section titles, card names, footer wordmark, section numbers, category tile labels, Last-Minute discount percentage | **ALWAYS UPPERCASE.** Line-height `0.95` (per Q48 signature). Letter-spacing `0.01em`. Single weight (400) — Anton is a single-weight display family. Size guidance: section titles 18–32px depending on viewport, hero 32–64px+. |
| **Figtree** | All body copy, card meta, review text, form fields, prices, counts, **eyebrows** (Q48 — Figtree 700 tracked `0.22em` uppercase coral), buttons, navigation, status pills | Weights 400/500/600/700 used. 700 for eyebrows + button labels + numeric tabular per Q43. Body line-height `1.5–1.6`. Italic restricted to pull quotes only. |

### Type scale

```css
--s-text-xs:   11px;   /* eyebrow labels UPPERCASE tracked (Figtree 700) */
--s-text-sm:   13px;   /* nav, buttons, card body (Figtree 600) */
--s-text-base: 16px;   /* body (Figtree 400/500) */
--s-text-lg:   17px;   /* lead paragraphs, hero sub (Figtree 500) */
--s-text-xl:   24px;   /* card headings (Anton uppercase) */
--s-text-2xl:  40px;   /* section titles (Anton uppercase) */
--s-text-3xl:  clamp(36px, 5vw, 64px);   /* dark-section section titles (Anton) */
--s-text-hero: clamp(64px, 9vw, 130px);  /* hero H1 (Anton) */
--s-text-partner: clamp(52px, 7vw, 96px); /* B2B block H (Anton) */
```

### Casing rules

- **Hero display:** ALWAYS UPPERCASE (Anton)
- **Section titles:** UPPERCASE (Anton) — Q48 signature anchors the brand fingerprint
- **Eyebrow labels:** UPPERCASE Figtree 700, tracked `0.18–0.22em`, 9–11px, coral `#C95A3A` (Q48 signature)
- **Buttons:** sentence case (Figtree 600/700), single verb default per Q25; UPPERCASE only inside Anton headlines
- **Card names:** Anton UPPERCASE per Q26 (e.g. STUDIO LINA, not Studio Lina)

### BANNED

- ❌ Inter, Roboto, Arial, system-ui as visible fonts (use Figtree); fallback only
- ❌ Anton in body copy, buttons, or anything below 14px
- ❌ Mixing Figtree weights in one line (pick 400/500/600/700, don't blend)
- ❌ Italic on Anton (display fonts don't italicize)
- ❌ Earlier locked values (Bebas Neue / Fraunces / DM Sans / Plus Jakarta / Outfit / Syne) — all retired per §20 Q23 + Q48. See decisions log for supersession.

---

## 3. SPACING (8-point grid)

Every margin / padding / gap is a multiple of **8px**.

**Allowed:** `4 · 8 · 12 · 16 · 20 · 24 · 32 · 48 · 64 · 80 · 96`
**Banned:** `5 · 7 · 9 · 11 · 13 · 15` — breaks the grid visually.

**Common patterns:**
- Section vertical padding: `80px` desktop, `64px` mobile
- Section horizontal padding: `48px` desktop, `20px` mobile
- Card inner padding: `14px 16px 18px` (asymmetric OK for cards)
- Grid gap (cards): `18px`
- Grid gap (tiles 6-col): `10px`
- Button padding lg: `16px 36px`
- Button padding md: `12px 26px`
- Button padding sm: `8px 18px`

**Max content width:** `1200px`, centered.

---

## 4. RADII

| Token | Value | Use |
|-------|-------|-----|
| `--r12` | 12px | Inputs, small buttons |
| `--r16` | 16px | Trust strip |
| `--r18` | 18px | Search bar outer |
| `--r20` | 20px | **Default card radius** (salon, review, lm, stat, city, slot panel, partner, insta) |
| `--r99` | 99px | All pills · chips · CTA buttons · avails · tags |

**BANNED:** sharp `0px` corners, `rounded-lg/xl/2xl` (Tailwind leftovers), blob organic shapes **except** Instagram tiles (see §7).

---

## 5. SHADOWS (Apple 5-level, warm-tinted)

**Every shadow uses `rgba(26,18,9,x)` tinting — NEVER `rgba(0,0,0,x)`.** Two-layer shadows minimum on md+.

```css
--sh-xs:    0 1px 2px rgba(26,18,9,.06);
--sh-sm:    0 1px 3px rgba(26,18,9,.07),  0 2px 8px  rgba(26,18,9,.05);
--sh-md:    0 2px 4px rgba(26,18,9,.08),  0 4px 16px rgba(26,18,9,.06);
--sh-lg:    0 4px 8px rgba(26,18,9,.09),  0 8px 32px rgba(26,18,9,.07);
/* --sh-xl RETIRED 2026-04-22 per Q11 — max shadow is now --sh-lg. Hero-level moments use custom inline declaration. */
--sh-coral:   0 2px 4px rgba(232,98,74,.25), 0 4px 16px rgba(232,98,74,.15);
--sh-coral-h: 0 4px 8px rgba(232,98,74,.32), 0 8px 28px rgba(232,98,74,.22);
--sh-amber:   0 2px 4px rgba(212,135,10,.22),0 4px 16px rgba(212,135,10,.14);
--sh-pressed: 0 1px 1px rgba(26,18,9,.12),  inset 0 1px 2px rgba(26,18,9,.06);
```

### When to use which — CRITICAL (we had "too high shadow" issues before)

| Element | Rest | Hover |
|---------|------|-------|
| Badges, pills, tags | `--sh-xs` | same |
| Nav pill, buttons | `--sh-sm` | `--sh-md` |
| Salon cards, review cards | `--sh-sm` | `--sh-lg` (NOT xl) |
| Category tiles, insta tiles | `--sh-sm` | `--sh-lg` (tiles are decorative, but no `--sh-xl` per Q11) |
| Stat glass, booking glass | `--sh-md` | same |
| Partner block (solid plum — Q16 2026-04-22) | inline custom shadow | — (hero-level feature, declared inline: `0 12px 40px rgba(74,30,60,.22)`) |
| Modal / bottom sheet | `--sh-lg` | — |

**Rule (Q11 lock 2026-04-22):** `--sh-xl` token DELETED from the system. Max shadow available is `--sh-lg`. Hero-level moments (max 1 per page) use inline custom shadow declarations — never a reused token.

---

## 6. GLASS — ONLY in 3 places

We had a problem with glass everywhere. Strict rule now: glass appears ONLY in these 3 contexts.

```css
--g1-bg:   rgba(250,246,239,.82);  --g1-blur: blur(28px) saturate(1.3);   /* nav pill only */
--g2-bg:   rgba(255,255,255,.62);  --g2-blur: blur(16px) saturate(1.2);   /* hero card overlay + booking summary */
--g3-bg:   rgba(250,246,239,.50);  --g3-blur: blur(8px);                  /* trust strip only */
--gb:      rgba(255,255,255,.55);                                         /* glass border */
```

**Allowed glass:**
1. **Nav pill** (`.nav-inner`) — G1, top floating glass
2. **Hero card photo overlay** (`.hv-overlay`) + booking summary panel — G2
3. **Trust strip** (`.trust-strip`) — G3, subtle

**BANNED glass on:**
- Content cards (salon cards, review cards, last-minute cards → solid white/plum)
- Section backgrounds
- Modals where not necessary
- Buttons

Always pair `backdrop-filter` with `-webkit-backdrop-filter`.

---

## 7. BLOBS & DECORATIVE SHAPES — STRICT

We had "blobs in every section" rot. New rule: blobs are decorative and reserved.

### Allowed blob usage

1. **Hero section** — max 3 soft background blobs (coral, blue, amber at 8-15% opacity)
2. **Dark sections** (Last Minute plum, Quartier ink, Footer ink) — max 2 blobs for depth
3. **Partner block** (now solid plum per Q16) — up to 2 decorative circles in `rgba(255,255,255,.08)` for depth, no gradient fill
4. **Instagram tiles** — THE blob shapes themselves (organic border-radius on each tile is the visual identifier)

### BANNED blob usage

- ❌ Every section does NOT get blobs
- ❌ Cards section, categories section, reviews section, stats — **no blobs**
- ❌ Light sections with white cards already pop — no additional "background color blobs"

### Q16 (2026-04-22) — No decorative gradients anywhere

Solen's look is **Swiss-editorial magazine**, not SaaS-gradient. Every decorative gradient is replaced with a solid color block + strong typography. Functional gradients (image text-legibility overlays) are preserved.

| Element | Was | Now |
|---------|-----|-----|
| Partner B2B block | `linear-gradient(135deg, amber → coral → plum)` | **Solid `plum` bg** · amber eyebrow · Bebas headline (white) · coral CTA · up to 2 decorative `rgba(255,255,255,.08)` circles for depth |
| Deals banner | `linear-gradient(135deg, peach → peach-light)` | **Solid `amber` bg** · Bebas big "12" numeral on left · white title + ink sub · coral CTA on right |
| Category tiles | Per-category gradient bg | **Real photo** when available · fallback = solid per-category color (Coiffeur amber / Barber plum / Nails coral / Spa sage / Makeup sand / Waxing blue) with Bebas category name centered |
| Hero visual card (salon photo placeholder) | `linear-gradient(145deg, amber, coral)` | **Solid `sand`** with Bebas salon name at 15% opacity (or letter initial large). Real photo replaces when loaded. |
| Map teaser | `radial-gradient(coral-subtle + sage-subtle)` | **Solid `coral-subtle`** bg · large lucide map icon centered (coral, 60% opacity) · Bebas "KARTE" label · green CTA pill |

### PRESERVED — functional text-legibility overlays
- Salon card photo bottom gradient (for badge contrast on photos): transparent → `rgba(26,18,9,.45)` over bottom 20% of image, ONLY used if badges sit on photo
- Discovery card overlay: transparent → `rgba(26,18,9,.78)` over bottom 40%, for white-text-on-photo legibility
- Review card top-left accent: not a gradient (was never one)

**Rule:** A gradient is allowed ONLY if the alternative is unreadable text on a photo. Pure decoration = denied.

### Instagram tile blob shapes (keep these — they're the signature playfulness)

```css
/* Each tile gets a unique organic border-radius */
.insta-tile-1 { border-radius: 40% 60% 70% 30% / 40% 50% 60% 50%; transform: rotate(-2deg); }
.insta-tile-2 { border-radius: 60% 40% 45% 55% / 50% 60% 40% 50%; transform: rotate(2deg);  }
.insta-tile-3 { border-radius: 50% 50% 40% 60% / 60% 40% 60% 40%; transform: rotate(-1.5deg); }
.insta-tile-4 { border-radius: 40% 60% 55% 45% / 30% 30% 70% 70%; transform: rotate(1.2deg); }
```

---

## 8. GRAIN OVERLAY (keep, subtle)

Desktop-only SVG noise, fixed, 3.8% opacity, multiply blend. Gives the whole page a warm magazine-paper feel. Hidden on mobile (performance, rarely noticed).

```css
.grain {
  position: fixed; inset: 0; pointer-events: none; z-index: 999;
  opacity: .038; mix-blend-mode: multiply;
  background-image: url("data:image/svg+xml,%3Csvg ...fractalNoise baseFrequency='.72' numOctaves='4'/%3E");
}
@media (max-width: 768px) { .grain { display: none; } }
```

---

## 9. CARDS

### Salon card (SQUARE cover photo — locked 2026-04-20)

```
┌───────────────────┐
│ [Badge top-left]  │
│                   │  ← aspect-ratio: 1/1 SQUARE
│              [♡]  │  ← heart top-right
│                   │
│      AMARA        │  ← Bebas Neue 56px text over gradient
│                   │
└───────────────────┘
Salon Amara          ★ 4.9
Kleinbasel · 28 Bewertungen
[Heute 14:30 frei]       ← sage availability pill
Ab CHF 45
[Coiffeur] [Farbe]       ← category tags
```

**Dimensions:**
- Image: `aspect-ratio: 1/1` (SQUARE — not 160px height, not 3:2, not 4:3)
- Card wrapper: `--r20` border-radius, `var(--raised)` bg, `1px solid var(--b)` border, `--sh-sm` shadow
- Image padding text (before real photo): `56px` Bebas Neue, `rgba(255,255,255,.9)`

**Badges (top-left, on image):** Yellow "Solen Top Pick" default (`--yellow` bg, `--yellow-t` text, 10px Fraunces uppercase, `--r99`, `--sh-sm`).

**Heart (top-right, on image):** 34px circle, `rgba(255,255,255,.75)` bg + 8px blur, `--r99`, 14px lucide heart outline. Hover → `rgba(255,255,255,.96)`, scale 1.12.

**Availability pill:** `--sage-s` bg, `--sage-t` text, 12px DM Sans 600, `--r99`, with clock icon.

**Tags:** 10px Fraunces 700 UPPERCASE, `--r99`, tinted per category (yellow for Coiffeur, blue for Farbe, plum for Barber, amber for Rasur, sage for Massage, coral for Spa).

**Hover:** `translateY(-3px)`, shadow `--sh-xs → --sh-sm`, border `--b → rgba(232,98,74,.4)` (coral/40), image scales 1.03 (500ms) — Q11 lock: no `--sh-xl` anywhere.

### Review card
- White bg, `--r20`, `1px --b` border, `--sh-sm` → `--sh-lg` on hover
- 14px DM Sans italic quote
- Avatar 32px circle with colored bg + initials
- 12px Fraunces name + 11px DM Sans location

### Last-Minute card (on plum section)
- `rgba(74,30,60,.92)` bg + G2 blur, `1px rgba(255,255,255,.10)` border, `--r20`
- Yellow `--yellow` Bebas Neue discount (`-40%` style, 56px)
- Two overlapping decorative circles inside (c1 top-right, c2 bottom-left) — these are "on the card" blobs and ALLOWED
- Coral time pill

### Quartier card (on dark ink section)
- `rgba(255,255,255,.06)` bg, `1px rgba(255,255,255,.10)` border, `--r20`
- 15px Fraunces 700 name, 12px DM Sans count
- Giant Bebas Neue index number at bottom-right corner, `rgba(255,255,255,.05)` — 64px, a ghost

---

## 10. BUTTONS

All buttons = Fraunces 700 UPPERCASE, `0.04em` tracking, `--r99` pill.

| Variant | Bg | Text | Shadow | Hover |
|---------|-----|------|--------|-------|
| `btn-coral` | `--coral` | #fff | `--sh-coral` | bg `--coral-h`, shadow `--sh-coral-h`, translateY -1px |
| `btn-amber` | `--amber` | #fff | `--sh-amber` | bg `--amber-h`, slight lift |
| `btn-ink` | `--ink` | `--bg` | `--sh-sm` | bg `--plum`, shadow `--sh-md`, lift |
| `btn-outline` | transparent | `--ink` | `--sh-xs` (1.5px `--b3` border) | bg `--ink`, text `--bg` |
| `btn-ghost-sm` | `--raised` | `--ink2` | `--sh-xs` (1px `--b` border) | bg `--sur` |

**Sizes:**
- `sm`: 8×18, 11px
- `md`: 12×26, 13px
- `lg`: 16×36, 14px

**Active state (tap):** `--sh-pressed`, `translateY(1px)`.

---

## 11. SEARCH BAR (Fresha pattern)

> **⚠️ RETIRED 2026-05-03** — This section describes a horizontal-segmented search pattern explicitly banned by LIVE_TRUTH §7 (Q49 stacked Was/Wo/Wann is the locked replacement: "Do NOT replace with horizontal segmented search bar"). Also references DM Sans (retired font per LIVE_TRUTH §6). Kept here for historical context only. **Do not implement this pattern.** See `_tasks/SOLEN_LIVE_TRUTH.md` §7 for current hero search spec.

Segmented horizontal pill with coral CTA at the end.

```
┌─────────────────────────────────────────────────────────────────────┐
│ KATEGORIE │ QUARTIER  │ WANN   │ UHRZEIT   │   ┌─────────┐         │
│ Coiffeur  │ Wo in... │ Heute  │ Beliebig  │   │ 🔍 Suchen│         │
└─────────────────────────────────────────────────────────────────────┘
```

- Outer: `--raised` bg, `1.5px var(--b2)` border, `--r18`, `--sh-lg` + inset highlight
- Segments: 68px min-height, `1px --b` dividers, 10px amber UPPERCASE label + 13px DM Sans 500 value
- CTA: coral pill, `--r12` (rounded but not fully pill), 16×22 padding, 8px margin from outer edge

**Mobile:** stacks vertically, segments separated by bottom borders, CTA full-width at bottom.

---

## 12. SECTION PATTERNS

> **⚠️ RETIRED 2026-05-03** — This section references Bebas Neue (retired display font, replaced by Anton per Q23/Q48) and Fraunces (retired headline font, replaced by Anton+Figtree split). The homepage section order described here is also stale (predates the V2 rebuild starting 2026-05-03). Kept for historical context. See `_tasks/SOLEN_LIVE_TRUTH.md` §6 (typography) and `_tasks/V2_REBUILD_LOG.md` "In-flight" section for the current homepage rebuild.

Every section uses the `.sec` wrapper (`position: relative; overflow: hidden`) and `.w` inner (`max-width: 1200px; margin: 0 auto; padding: 0 48px`).

Every section header:
```
<span class="sec-eye">EYEBROW — amber, 11px, tracked 0.22em</span>
<h2 class="sec-h">Section title — Fraunces 800, 40px</h2>
```

**Homepage section order:**
1. **Hero** — Bebas Neue headline left + hero visual right (square photo card + floating glass stat)
2. **Search bar** — Fresha pattern, max-width 780px
3. **Stats band** — 3 glass stat tiles, `--sh-md`
4. **Categories** — 6 gradient tiles, 1:1 aspect, Bebas Neue name over gradient
5. **Salon cards grid** — 3-col desktop, filter pills above
6. **Slot selection + booking summary** — two-column, left solid panel + right glass summary
7. **Last Minute** (plum bg) — 3 dark cards with discount, limited circles/blobs OK
8. **Reviews** — 3 solid white cards with italic quote + avatar
9. **Instagram tiles** — 4 blob-shaped tiles with Bebas Neue text (coral/amber/plum/sage)
10. **Quartier** (ink bg) — 4-col neighborhood cards with ghost index numbers
11. **Partner** gradient block (amber→coral→plum)
12. **Trust strip** — G3 glass, single line
13. **Footer** — ink bg, 3-col, Bebas Neue logo with coral+amber accent

---

## 13. ANIMATION

```css
--e:  cubic-bezier(.4,0,.2,1);    /* standard in/out */
--eo: cubic-bezier(0,.55,.45,1);  /* bounce out for reveals */

--dur-fast:  150ms;  /* hover bg, color, pill select */
--dur-mid:   250ms;  /* buttons, card shadow transitions */
--dur-slow:  400ms;  /* card hover lift, image zoom, reveal fade-in */
```

**Card hover:** `translateY(-5px)` + shadow upgrade, 400ms `--e`.
**Reveal on scroll:** opacity 0→1 + translateY 20px→0, 400ms `--eo`, stagger children 80ms.
**Heart bounce:** keyframe `1 → 1.3 → 0.9 → 1.1 → 0.95 → 1` over 550ms.
**Press:** `scale(0.97)` + `opacity: 0.8` for 100ms (`.solen-press-effect`).

**`prefers-reduced-motion: reduce` — MANDATORY.** Disable all transforms, keep color transitions only.

---

## 14. IMAGERY

- Warm, cinematic, slightly desaturated (NOT bright stock-photo energy)
- No AI-grain default (use only `grain` overlay)
- Salon cover photo aspect: **1:1 SQUARE** (locked) — NOT 3:2, NOT 4:3
- Category tile aspect: **1:1**
- Discovery card aspect: **3:4 portrait**
- Fallback gradient per category (coiffeur=amber→coral, barber=plum→blue, nails=coral→yellow, spa=sage→blue, makeup=sand→coral, waxing=plum→sage)

---

## 15. DARK MODE

**Not in scope.** No `[data-theme="dark"]`, no `prefers-color-scheme: dark` overrides. Single warm light theme.

---

## 16. ICONS

- **Primary:** `lucide-react` outlined, 1.5–1.8 stroke weight, 14–22px
- **Custom category icons:** hand-tuned SVG components (Coiffeur, Barber, Nails, Spa, Makeup, Waxing) in the same outlined style
- Filled icons ONLY for: star ratings, active heart
- **BANNED:** emoji as functional UI (use SVG), unicode as icons, font-awesome, heroicons

**Exception:** `·` (middle dot) as metadata separator ("Basel · ★ 4.9 · 28 Bewertungen").

---

## 17. VOICE

- **Tone:** confident, warm, direct, Swiss. Not chirpy. Not corporate.
- **Language:** German primary (Baseldeutsch-aware), then English, French, Italian.
- **Person:** "du/dein" to the customer; "wir" sparingly (we're a platform, not a corporation).
- **Numbers:** tabular nums (`.data-text`). Currency: `CHF 85`. Ratings: `4.8`. Counts: `(127)` parenthesized.

**Example copy:**
- Hero: "BEAUTY. DIREKT GEBUCHT." · dynamic eyebrow "Von der Schweiz. Für dich." (Q5 2026-04-22)
- Subtitle: "Der erste Salon-Booking-Service, der wirklich zu Basel passt."
- CTA: "Salon finden", "Jetzt buchen", "Termin buchen"
- Sections: "Top bewertet in Basel", "Spare bis zu 50%", "Was Basel sagt", "Entdecke Basel"
- Footer tagline: "Der erste Salon-Booking-Service, der wirklich zu Basel passt. Von uns. Für euch."

---

## 18. WHAT'S BANNED — full list

| ❌ Banned | ✅ Replacement |
|-----------|----------------|
| `#000000` black | `--ink` (`#1A1209`) |
| Cool greys | warm ink opacity |
| Green+peach V2 palette | Coral/amber/plum/sage (this doc) |
| Anton in body/buttons | Figtree body, Figtree 600/700 buttons |
| Inter / Roboto / Arial | Anton (display) + Figtree (body) per §2 |
| Italic on display font | Figtree italic only for pull quotes; Anton stays roman |
| Bebas Neue / Fraunces / DM Sans / Plus Jakarta / Outfit / Syne (any earlier-locked font) | Anton + Figtree per §2 (retired per §20 Q23 + Q48) |
| `rounded-lg/xl/2xl` (Tailwind leftover) | `--r20` card, `--r99` pill |
| 0px sharp corners | `--r12` minimum |
| 3:2 cover photos on cards | 1:1 SQUARE (this doc) |
| V5 zones (Zone 1/2/3/4) | Single unified design language |
| Glass on content cards | Solid `--raised` cards |
| Glass on section bgs | Solid `--bg` / `--sur` |
| Blobs in every section | Hero + dark sections + Instagram tiles ONLY (§7) |
| `--sh-xl` on regular card hover | `--sh-lg` max on hover |
| Cream `#FAF6EF` page bg (Q15 RETIRED 2026-04-22) | White `#FFFFFF` page bg |
| "Von Basel. Für Basel." hyperlocal voice (Q5 RETIRED 2026-04-22) | "Für [detected city]" dynamic, fallback "Für deine Stadt" (Swiss-wide) |
| 4-segment search bar (Q4 RETIRED 2026-04-22) | 3-segment: Was · Wo · Wann |
| `--sh-xl` token (Q11 RETIRED 2026-04-22) | Max is `--sh-lg`, hero moments use inline custom |
| `--sh-coral` colored-glow token (Q16 RETIRED 2026-04-22) | Use `--sh-sm` warm-ink shadow |
| `--sh-amber` colored-glow token (Q16 RETIRED 2026-04-22) | Use `--sh-sm` warm-ink shadow |
| Decorative gradients on partner block, deals banner, category tiles, hero card, map teaser (Q16 RETIRED) | Solid color blocks + Bebas Neue + warm ink (Swiss-editorial) |
| `linear-gradient` / `radial-gradient` for decorative fills (Q16 RETIRED) | Solid colors. Functional dark-bottom overlays for text-on-photo legibility ARE preserved. |
| Dark mode | Single light theme |
| emoji as UI icons | lucide-react SVG |
| Availability pill removed V2 attempt | **Keep** availability pill (sage) |
| Old V2 `#F5A962` peach anywhere | coral `#E8624A` |

---

## 19. PREVIEW

**Active living preview:** `public/solen-coral.html` — open at `http://localhost:3000/solen-coral.html`

Every design decision in this doc is renderable in that file. When iterating in Phase 2, we edit that file directly + append to this doc's decisions log below.

---

## 20. DECISIONS LOG

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-04-20 | **Reverted from V2 green+peach attempt back to coral system** | V2 felt generic AI-beauty-app. Coral/cream/Bebas is distinctive. |
| 2026-04-20 | **Salon card cover photo = 1:1 SQUARE** (was 160px landscape) | Denser grid, matches Airbnb/Instagram visual language, image pops more |
| 2026-04-20 | **Glass restricted to 3 contexts** (nav, hero overlay + booking summary, trust strip) | Glass-everywhere looked dated; selective use feels premium |
| 2026-04-20 | **Blobs restricted to hero + dark sections + Instagram tiles** | "Blob in every section" was visual noise |
| 2026-04-20 | **`--sh-xl` cap: 2 places per page** | "Everything too high shadow" was user feedback |
| 2026-04-20 | **Bebas Neue kept** (hero, Instagram tiles, discount numerals, footer logo) | User said it's the playfulness — used correctly it works |
| 2026-04-20 | **Grain overlay kept** at 3.8% desktop-only | Magazine-paper warmth |
| 2026-04-20 | **Dark mode killed** | Single warm light theme, no `[data-theme="dark"]` |
| 2026-04-20 | **Fraunces replaces Syne as heading font** | User feedback: Syne read "too techy" / Y-Combinator-SaaS. Fraunces (variable serif with optical sizing) breaks the geometric-sans pattern, adds warm editorial personality, pairs beautifully with Bebas Neue display + DM Sans body. |
| 2026-04-20 | **Kill colored glow on default buttons** (`--sh-coral`/`--sh-amber` → `--sh-sm` rest, `--sh-md` + `brightness(1.05)` hover) | User feedback: "too much glow." Colored glow under same-color button = muddy halftone wash. `--sh-coral` and `--sh-amber` tokens retained for reserved one-off hero moments only. Search CTA also updated. |
| 2026-04-22 | **Q1: Salon card ratio locked at 1:1 square** | 20/19 vs 1:1 imperceptible to users. Square is the brand ratio across staff portraits, category tiles, salon cards. Consistent mark. |
| 2026-04-22 | **Q2: Price display `ab CHF 85` only** (no `$/$$/$$$` tiers) | Swiss consumers want specific CHF, not abstract American tiers. Concrete + honest > generic. |
| 2026-04-22 | **Q3: Swipeable image carousel on salon cards** (Airbnb pattern) | Photos are #1 booking factor in beauty. Carousel reveals 3-5 photos per salon; +40-60% engagement per Airbnb data. IMPLEMENTATION REQUIRED — not in current code. |
| 2026-04-22 | **Q4: Search bar = 3-segment pill (Was · Wo · Wann)** | GTM spec. Time-of-day is inside "Wann" bottom sheet. 4-segment version retired. Cleaner mobile, fewer thumb targets. |
| 2026-04-22 | **Q5: Voice = Swiss-wide, not hyperlocal** | Retire "Von Basel. Für Basel." → "Von der Schweiz. Für dich." Headlines dynamic to detected city ("Für Zürich", "Für Basel"). Fallback "Für deine Stadt". Platform supports any Swiss city from day 1. |
| 2026-04-22 | **Q6: Phase sequence — UI polish FIRST, payment second** | Site isn't live. Claude Design's whole point is to perfect the UI BEFORE launching. GTM Phase 1 priority aligns. |
| 2026-04-22 | **Q7: Moat priority — Chat intelligence + Allergy tags first** | Daily engagement + legal safety. Both non-optional once scale hits. AI nail art / Solen Score / disputes / gold pins / confetti follow in order. |
| 2026-04-22 | **Q8: TWINT integration required before Phase 3 launch** | ~70% Swiss adoption. Asking a CH customer to pay with credit card only = conversion killer. Stripe supports TWINT via Payment Methods API. |
| 2026-04-22 | **Q9: `/termine` → redirect to `/profile/bookings`** (don't duplicate code) | Preserve SEO + user bookmarks. Canonical is `/profile/bookings` (works, translated, auth'd). Moat's 440-LOC TerminePage not resurrected. |
| 2026-04-22 | **Q10: Add "Solen Favorit" as 4th badge** (yellow `#F2C144`, algorithmic curation) | Distinct from "Top bewertet" (rating-only). Algorithmic = rating × volume × reply rate × recency × response time. Priority order: Solen Favorit > Top bewertet > Beliebt > Neu. One badge per card. |
| 2026-04-22 | **Q11: `--sh-xl` token REMOVED from system entirely** | Every component thought it deserved hero treatment. Killing the token forces color + motion over shadow explosion. Max shadow is now `--sh-lg`. Hero-level moments use custom declaration, not a token. |
| 2026-04-22 | **Q12: Bebas Neue scope unchanged** (hero + Instagram tiles + discount numerals + footer logo) | Each context earned. Over-use dilutes the brand stamp. |
| 2026-04-22 | **Q13: Scraped profiles get "Claim this listing" ribbon + faint watermark** | Honest signal to customers ("not actively managed"), conversion hook for salon owners. Avoids sad-grey aesthetic. |
| 2026-04-22 | **Q14: Mobile bottom nav = 4 tabs** (Home · Entdecken · Suchen · Profil) | Locked. 3 too sparse, 5 too cramped on small iPhones. |
| 2026-04-22 | **Q15: Page bg reverted to WHITE `#FFFFFF` (not cream)** | User override. Cream retired in favor of clean white. Warmth preserved via coral + Bebas + Fraunces editorial moves, not via bg tint. |
| 2026-04-22 | **Q16: Decorative gradients killed across the system** | User feedback: "hate all the glow or gradient". Solen pivots from SaaS-gradient vibe to Swiss-editorial magazine aesthetic (solid color blocks + strong typography). FUNCTIONAL image-text-legibility overlays (e.g. dark gradient at bottom of photos so white text reads) are preserved — they're accessibility, not decoration. Applies to: partner B2B block (now solid plum), deals banner (solid amber), category tiles (real photos + solid-color fallback), hero visual card (solid sand), map teaser (solid coral-subtle). `--sh-coral` and `--sh-amber` colored-glow tokens also deleted. |
| 2026-04-20 | **Salon card shadow diet** | Rest `--sh-xs` (was `--sh-sm`), hover `--sh-sm` (was `--sh-xl`). Added: border goes coral/40 on hover, image scales 1.03 over 500ms, translateY(-3px). Color + motion replace shadow explosion. |
| 2026-04-20 | **Category tile shadow diet** | Removed `box-shadow` entirely (was `--sh-sm` rest → `--sh-xl` hover). Added 1px ink/06 border rest. Hover: border coral/40, scale 1.04, rotate(-1deg), filter saturate(1.1). Vibrant gradients create their own depth. |
| 2026-05-01 | **Q18: Disabled state = token swap (filled → `--ink4`, text/border → `--ink3`, `cursor:not-allowed`)** | Reads as deliberately "off duty" not "broken" or "loading." Brand-aware. Replaces the previous opacity-based ad-hoc disabled patterns. Applies to: buttons (filled bg → `--ink4` #A89C90, white text), outline buttons (border + text → `--ink4`), inputs (bg → `--sur`, text → `--ink4`, border → `--b`), pills (bg → `rgba(168,156,144,.18)`, text → `--ink4`), cards (bg → `--sur`, text → `--ink4`, photo → `grayscale(1) opacity(.5)`). On dark backgrounds use `rgba(255,255,255,.4)` text + `rgba(255,255,255,.18)` fill. |
| 2026-05-01 | **Q19: State lockset = 4 + split-empty (happy / empty-FTU / empty-filtered / error / loading)** | Every data-driven surface must implement all five before merge. "Skip" not universal — wizard-only, locked separately in Q56. Empty splits into two emotional registers: **first-time-user** (warm coral CTA, "Welcome to Solen / Browse Basel →") vs **filtered-to-zero** (neutral, recovery-flavored, "No matches / Clear filters"). Loading uses skeleton-shaped-like-real-content (not generic grey blocks). Error is alert + sub + retry CTA. Pattern: Notion / Linear / Airbnb search. |
| 2026-05-01 | **Q20: Loading default = skeleton (shape-of-real-content)** | Skeleton mirrors the happy-state layout (same thumb size, same line widths, same card spacing) so when real data arrives nothing shifts. Shimmer animation: linear gradient `200%` width, 1.4s loop. Consolidates 3 ad-hoc skeletons (`SalonPageSkeleton`, `CategorySkeleton`, generic `Skeleton`) into one shape-mirroring system; each card type owns its `Skeleton<X>` companion. Spinner reserved for inline button-state loading only (B variant). |
| 2026-05-01 | **Q21: Empty state grammar = brand illustration** (line-coral SVG, lucide-family stroke) | All empty states use a small custom SVG illustration (~120×90 viewBox) drawn in coral stroke 2.5 + warm-ink accents, matching Lucide line-art family. Pattern: tiny ground-shadow ellipse + main subject (line-only outline) + 2-4 personality details (eyes/smile/sparkle/etc). Headline (Figtree 17/700) + sub (Figtree 13/2 lines max) + coral CTA pill underneath. Required surfaces: Favorites, Bookings, Search-results-filtered, Reviews, Dashboard analytics. Each ~10-20 min to draw in-system. NO photo backdrops (those reserved for landing/marketing only, not in-app empty states). |
| 2026-05-01 | **Q22: Error grammar = inline (in-place block)** for fetch/load failures + **toast** reserved for action-result failures | Default: failing region replaced with coral-tint inline block (40px circle icon + 15/700 headline + 12/regular sub-2-lines + coral retry pill). Same shape as empty (Q21) and loading (Q20) — only message + icon change (Solen UI #4 Consistency). Toast (dark-ink bg, white text, 12-14px, 5s auto-dismiss, retry-action link) used ONLY when an action just submitted by the user fails: booking submit, save changes, copy link, favorite toggle. Full-page takeover (C) reserved for total outages (auth lost, API down). Banner (D) not adopted. Network error vs server-500 vs validation: same visual; copy differentiates. |
| 2026-05-01 | **Q23: Brand mood = Bold + Playful + Warm** (RE-LOCKED after Uber Eats research; supersedes earlier Calm + Premium + Warm) | Page bg WHITE always (Q15 respected). **Bold** = Anton 60px display headline + Figtree 700-800 weights for emphasis · confident copy ("Booked. Done." / "Find your next look this week" / "Skip the phone tag") · solid CTA pill, never ghosted by default. **Playful** = line-art accents (amber squiggle + coral/amber sparkles in mixed sizes) · soft-underline highlight behind em words · two-color em pattern (one coral em + one amber em per headline, never both the same color) · friendly chip pairs ("Live in 4 cities" coral + "Booked in 30 sec" amber). **Warm** = coral + amber as the ONLY accent palette (no green, no purple, no blue-cool tones in UI accents) · warm-ink `#1A1209` for body text (NOT pure black) · cards bring both colors in (amber rating star + coral time signal) · soft-warm `#8a7a6e` for secondary text. **Brand color discipline (critical):** coral is *signal*, NOT *substrate*. Coral fills the CTA pill, the eyebrow dot, the em underline-highlight, the time pulse — but body text, page bg, and structural surfaces stay neutral. Amber is the second accent (rating gold, secondary chip color, alternate em underline) — distinct enough from coral to read as "two colors" but warm enough not to clash. **NO colored shadows / no glow** (Q16 lock — `--sh-coral` / `--sh-amber` deleted, never reintroduce). Button states use brightness shift only (filter:brightness(0.94) hover, 0.88 active) + transform:scale(0.98) on press + ::after arrow translate. **NO mix-blend-mode for icons** (caused gray heart on light photos) — use literal white silhouette + 1px dark hairline drop-shadow for legibility. **Anti-patterns banned:** dark backgrounds, cream tinted bg, peach/color-block hero, blob shapes, decorative gradients, hue-shifted hover states (#D8553B-style "now it's a different color"). The earlier "Calm + Premium + Warm" lock was abandoned because *Calm* read as *forgettable* and *Premium* did no concrete work — replaced by *Bold + Playful* which both force visible decisions in motion, type, copy, and accent treatment. |
| 2026-05-01 | **Q24: Copy tone = Direct-friendly · NO EMOJIS anywhere in copy** | Voice register: explicit, helpful, neutral. Examples: "No favorites yet" / "Tap the heart icon on any salon to add it to your favorites." / "Your booking is confirmed!" / "Something went wrong — please check your connection and try again." Punctuation includes exclamation only on positive confirmations (booking confirmed, action saved). Em-dash for clauses. Sentence case for body, sentence case for buttons. **Strict no-emoji rule** applies to all UI copy: empty states, errors, toasts, button labels, tooltips, push notifications, transactional emails. Reasoning: emojis clash with Q23 mood (Calm + Premium) and translate inconsistently across DE/FR/IT. Editorial-warm (A) deferred — re-evaluate at Phase 2 once design system maturity supports it. Localization: same warmth, no emoji shortcuts. |
| 2026-05-01 | **Q25: Button label grammar = single verb DEFAULT + expand with specific detail when stakes are concrete** | Default: terse single verb ("Save" / "Confirm" / "Cancel" / "Resend" / "Retry" / "Delete"). **Expand with the specific detail** whenever a number, name, time, or countable thing is being committed to — these labels visibly carry that information into the tap moment. Required-expand cases: **money** ("Pay CHF 65", "Refund CHF 32"), **time commitment** ("Confirm Thu 14:30"), **multi-item action** ("Delete 3 bookings"), **named target** ("Block @marie", "Cancel Coiffeur Marie booking"). Pure single-verb is correct for: profile save, password reset confirm, simple yes/no in low-stakes flow. Anti-patterns banned: "Continue" / "Submit" / "OK" as primary CTAs (they offload context to the user). Branded phrasing ("Lock it in", "Pin this") banned — clashes with Q24 direct-friendly. Localization: DE/FR translations follow the same rule (e.g. "Bezahlen CHF 65", "Buchung am Do. 14:30 bestätigen"). |
| 2026-05-01 | **Q26: Card hierarchy = Airbnb-style — square rounded photo with ALL features ON the photo, BARE text below (no card body, no border)** (final lock; supersedes earlier image-canvas + body-bar variants) | Card width **220px desktop / 170px mobile**. **Photo:** 1:1 square (Q1 lock), 14px rounded corners, hover scale 1.02. **Photo chrome (all overlaid on photo, never on text):** (1) Top-left **badge pill** when present — solid white bg + ink text by default, `#F2C144` yellow for Solen Favorit (Q10), Figtree 10/700 letter-spacing .02em, padding 5/10, soft shadow. (2) Top-right **heart button** — 30×30 frosted-white circle (`rgba(255,255,255,.9)` + 6px backdrop blur), coral icon when unsaved, solid coral fill + white icon when saved, hover scale 1.1. (3) Bottom-left **live availability pill** when relevant — coral solid bg, white text, white pulsing 5×5 dot, single consistent style (no color variants), copy like "3 spots today" / "Last spot 14:30" / "Open now". (4) Bottom-center **carousel dots** for Q3 swipe — small white pills 5×5, only shown on multi-photo cards AND only when no live pill present (live pill hides dots to avoid bottom-edge clutter). **NO text on the photo. NO scrim/gradient over the photo. NO card body, NO border, NO white frame.** **Bare text under the photo (10px top, 2px x-padding):** Row 1 = name (Figtree 14/700, ellipsis 1-line) + ★rating (Figtree 12/600). Row 2 = meta single-line (Figtree 12/400 ink3: `Basel · 0.4 km · 142 reviews`, ellipsis). Row 3 = price (Figtree 13/700 ink, "ab CHF" eyebrow 13/400 ink3). **Type system:** 4 sizes (14/13/12) × 2 weights (700/400-600). Hierarchy via contrast (Solen UI #9b). **Layout:** horizontal `scroll-snap-type: x mandatory` rail, gap 14px, hidden scrollbar. Replaces drifted `aspect-[4/3]` patterns in `components/booking/ConfirmationStep.tsx:72` and `components/dashboard/GalleryManager.tsx:181`. **Earlier same-day variants (image-canvas full-width + adaptive frosted glass; compact-with-white-body-bar) rejected** — first had legibility issues from text-on-photo overlay; second had unnecessary chrome bar under the photo. Airbnb pattern is the correct anchor: photo is the product, text is the caption. |
| 2026-05-01 | **Q28: Numeric typography = tabular-nums on movers, proportional default** | Default: Figtree's natural proportional digits. **Apply `font-variant-numeric: tabular-nums` (or Tailwind `.tabular-nums` utility class) only to:** (1) numbers that change in place — booking countdowns, live availability counters, animated values, cart totals updating; (2) sortable / vertically-aligned columns of numbers — search results sorted by price, dashboard metric stacks, comparison tables. Everywhere else (card meta lines like "★ 4.8 · 0.4 km · 142 reviews", inline prices in flowing copy, single-instance prices) stays proportional. Reasoning: jitter in moving numbers is the only visible failure mode; flowing prose reads more natural with proportional digits. Ship as a single Tailwind utility class — no font dependency, no payload cost. Already present in `app/globals.css:176` for prior usage. Anti-pattern: standalone DM Mono / JetBrains Mono / mono font for numerics — Figtree's tnum feature does the job. |
| 2026-05-01 | **Q30: Shadow scale = 3 tiers (sm / md / lg) + border-only for elevation-0** (skill-aligned, supersedes the original 5-level "Apple" scale in §5) | Cleaned per `_rules/SOLEN_UI.md` §5d (borders > shadows for in-flow) and the Shadows heuristic ("≥16px blur, ≤10% opacity"). **The 4 elevation tiers:** **(0) border-only** = `border: 1px solid rgba(26,18,9,.08)`, NO shadow — for in-flow cards (salon cards, review cards, list items, anything adjacent to other cards). **(1) sm** = `0 2px 8px rgba(26,18,9,.06)` — card hover, dropdowns, light popovers, search suggestions. **(2) md** = `0 4px 16px rgba(26,18,9,.08)` — sticky bottom CTAs, floating cart pill, popovers, sticky filter bars. **(3) lg** = `0 8px 32px rgba(26,18,9,.10)` — modals, bottom sheets, full-screen overlays. **Hard ceilings:** all values warm-tinted neutral `rgba(26,18,9, x)` (NEVER `rgba(0,0,0, x)` cold black, NEVER `rgba(brand, x)` colored). Max blur 32px (lg). Max opacity 10%. Single-layer (drop the old 2-layer convention). **Retired:** `--sh-xs` (replaced by border-only — too tight at 2px blur, violated heuristic), `--sh-xl` (Q11), `--sh-coral`/`--sh-amber` colored glows (Q16). Tailwind config `tailwind.config.js` to be migrated: delete `coral-glow`, `coral-glow-hover`, `amber-glow`, `warm-xl` aliases; remap `card`/`card-hover` to new sm; add new `sh-md` / `sh-lg`. |
| 2026-05-01 | **Q32: Spacing scale = 9 sanctioned + 2 hero-only extras** (`4 / 8 / 12 / 16 / 20 / 24 / 32 / 48 / 64` + `80 / 96`) | Skill-aligned per `_rules/SOLEN_UI.md` Spacing ("4-point system. Everything in multiples of 4"). 9 core values cover 99% of UI: card padding (16), card-hover lift (4), inter-card gap (12), screen edge (16 mobile / 24 desktop), section vertical (64). 80/96 reserved for hero/landing sections only — non-hero components reaching for these is a smell. **Banned values:** 5, 7, 9, 11, 13, 15, 18, 22, 28, 36, 40, 44 (any non-multiple-of-4). Naming convention: `scale-N` where N = the multiple (scale-3 = 12px, scale-16 = 64px). Migration target for `tailwind.config.js`: align spacing keys to this set, remove arbitrary `p-[5px]` / `gap-[7px]` patterns from existing components. |
| 2026-05-01 | **Q35: Inter-screen transitions = SLIDE as default (C) + SHARED-ELEMENT MORPH for the home-card → salon-detail moment only (D)** | **Default route transition (C — slide):** new screen slides in from right (`translateX(100%)` → `translateX(0)`), current screen pushes left + dims slightly (`translateX(-25%)` + `opacity .65`). Duration 450ms. Easing `cubic-bezier(0.2, 0.8, 0.2, 1)` (default out-curve per SOLEN_UI Animation). Reverse direction on back-nav. iOS-native pattern. Implementation: native `View Transitions API` (`document.startViewTransition`) where supported (Chrome shipping, Safari TP, Firefox flag), CSS fallback for older browsers. **Hero moment (D — shared-element morph):** ONLY the home-feed-card → salon-detail-page transition. The card photo becomes the detail-page hero (size 120×120 → full-width 160px tall, border-radius 14 → 0, 600ms cubic-bezier-out). Other shared elements (salon name, rating) optional follow-along. Implementation: `view-transition-name` per-element + `View Transitions API`, OR Framer Motion `layoutId` if VT API unavailable. **Anti-patterns:** A (hard cut, no transition) — only acceptable for tab-bar swaps within bottom nav. B (fade only) — never as primary route transition; carries no direction. Morph applied to non-card transitions (settings, profile, etc.) — wrong tool for non-shared-element moments. **Reduced-motion:** all transitions respect `prefers-reduced-motion: reduce` → fall back to instant cut + 100ms opacity for screen swap. **Cost:** slide ~free (10 lines CSS or native VT API). Morph ~1hr lib setup + per-route shared-element wiring on home-card + salon-detail-hero only. |
| 2026-05-01 | **Q36: Celebration moments = 5 sanctioned actions get a micro-interaction; everything else stays flat** | **The 5 moments that earn celebration:** (1) **Booking confirmed** — coral ring expands outward (halo, 700ms ease-out) + checkmark scales-in with overshoot (cubic-bezier(0.4, 1.4, 0.5, 1), 100ms delay) + dark-ink toast slides up from bottom 300ms later ("Buchung bestätigt — Do 14:30"). (2) **Payment success** — shares the booking-confirmed treatment (high-stakes financial moment, deserves identical polish). (3) **Favorite added** — heart fills `#FF4A6B` (literal love-red, NOT brand token) + scale pop 1 → 1.32 → 0.94 → 1 over 600ms with cubic-bezier(0.4, 1.5, 0.5, 1) overshoot + 4 tiny coral particles burst outward in 4 diagonal directions. (4) **Loyalty stamp earned** — new stamp lands with scale-up + slight rotation wobble (0 → 1.25 → 0.9 → 1 + -12° → +2° → -1° → 0°) over 600ms, coral fill activates simultaneously. (5) **Review submitted** — small dark-ink toast slide-up + ★ stars fill in sequence (50ms stagger). **NO celebration on:** generic save · message sent · search submit · favorite REMOVED (no anti-celebration) · profile edit save · pagination · filter change · low-stakes confirms. **Per SOLEN_UI #9d:** "Skip the emotional layer on utility screens, high-frequency repetitive actions, and anywhere it would slow down a pro user." Animation library: Framer Motion or native CSS animations + `prefers-reduced-motion` fallback (instant state change, no animation). **Cost:** ~3hr to build 5 micro-interaction components; reusable across all surfaces. |
| 2026-05-01 | **Q40: Hover scope = ONLY 4 sanctioned element classes get a hover state** | The 4 hover-eligible classes: (1) **CTA buttons** — brightness shift per Q23 (filter:brightness(.94) hover, .88 active) + ::after arrow translate 2px right. (2) **Text links** — color shift to coral, underline-thickness 1.5px+ on hover. (3) **Interactive cards** (salon card, review card, list-row card) — `transform: translateY(-2px)` + border color shifts to `rgba(232,98,74,.4)` (coral/40). 200ms ease-out. (4) **Nav icon buttons** (header nav, bottom-nav active state when on desktop) — frosted white background fill on hover. **NO hover on:** chips, pills, photos, icons inside cards, stars/ratings, badges, tags, tab labels, breadcrumbs, footer links. Reasoning: mobile is ~70% of Solen traffic — hover is desktop-bonus, never load-bearing. Designing hover for elements mobile users never see (chips, pills, photos) creates inconsistency between platforms and wastes design budget. The 4 sanctioned classes are the ones users tap-target deliberately, so hover-as-tap-preview makes sense. Wildcard hover effects (parallax tilt on cards, color-wave fills, etc.) explicitly banned per SOLEN_UI #5. |
| 2026-05-01 | **Q41: Title truncation = NONE — full text always shown in every language** (revised, supersedes earlier surface-aware lock) | **No truncation anywhere.** Salon names, button labels, error messages, chip labels, meta lines — all render in full in EN, DE, FR, IT. Cards expand vertically (allow up to 3-4 lines on long Swiss names like "Le Salon de Beauté Kleinbasel — Frisuren & Maniküre"). List rows wrap to 2-3 lines as needed. Buttons reflow (no nowrap, text-align center). Chips wrap to next row via flex-wrap (never truncate — losing "Jetzt geöffnet" → "Jetzt…" loses meaning). **Reasoning:** truncating ANY language is information loss. DE/FR/IT users (most of Solen's market) would see ellipsis far more often than EN users — design that punishes the majority languages is bad design. Card heights varying slightly is preferable to information loss. **Anti-pattern banned:** any `text-overflow: ellipsis` on user-facing strings · `-webkit-line-clamp` on names · marquee scroll · hover-to-reveal full text. **Implementation:** drop the `.truncate-card` / `.truncate-row` Tailwind utilities entirely. Default = full text wrapping. Card layouts must absorb 3-4 lines of name without breaking. Detail-page headings already had no constraint — keep that. **Edge case:** if a salon name is genuinely hostile (>80 chars), product can enforce server-side max length in the merchant onboarding flow rather than truncating in the UI. |
| 2026-05-01 | **Q42: DE/FR/IT text expansion = full text always, never truncate** | All UI strings render in full across EN / DE / FR / IT — no truncation anywhere (per Q41 revision). **Reflow rules per element:** **Buttons** → wrap to 2 lines (no nowrap, text-align center, line-height 1.25, max-width 220-260px). **Error sub-copy / hero subhead / modal body** → wrap to 3-4 lines (no clamp, line-height 1.4-1.5). **Filter chips / badge stacks / tag clusters** → flex-wrap to next row (never truncate — losing label = losing meaning). **Card titles** → wrap to 3-4 lines, card height absorbs (Q41 revised). **Card meta lines** → wrap to 2 lines if needed (no ellipsis). **List-row titles** → wrap to 2-3 lines (no ellipsis). **Numeric values** (prices, distances, times) → no wrap, kept as single tokens. **Labels in tight forms** → label sits ABOVE field (vertical stack), so DE long labels don't crash field width. **Implementation:** test all components in DE first (worst case for length) — if it works in DE it works in EN/FR/IT. Server-side merchant onboarding enforces sane salon name max length (~80 chars). **No CSS utility "truncate" exists in Tailwind config** — actively delete from any drift if found. |
| 2026-05-02 | **Q43: Numeric alignment = tabular only on movers, proportional in flowing text** | **6 contexts, 6 rules.** (1) **Card price (single, in-flow card body)** → left-aligned, `font-variant-numeric: proportional-nums`. Reads natural — no need for digits to align across rows. Format: `CHF 65`. (2) **Sortable list / price column (lists, dashboards, service menus)** → right-aligned, `tabular-nums`, decimals always shown (`CHF 45.00`, `CHF 125.00`). Eye scans column without re-anchoring. (3) **Form price input** → left-aligned, currency prefix in pill (`CHF` separator-pill on grey `#FAF7F3`, then number). Number stays `proportional-nums` — user is typing, reads as text. (4) **Cart totals / receipt / invoice** → right-aligned, `tabular-nums`, decimals on every line including subtotals/tax (`CHF 100.16`). Total row bolds + bumps to 15px, otherwise identical alignment so all decimal points stack. (5) **Live counter / animated number (slot count, message badge, etc.)** → left-aligned next to label, `tabular-nums` so digits don't shift width when value changes. (6) **Hero number (Last-Minute %, brand stat, big visible metric)** → center-aligned, Anton 60-84px, `tabular-nums` so cycling values don't jiggle. **Currency rule:** prefix `CHF` always sits BEFORE the number (Swiss convention), separated by a thin space in cart contexts, by a regular space in card body. **Anti-patterns banned:** tabular-nums applied globally · decimals shown on card-body prices when value is whole (`CHF 65.00` reads bureaucratic in a card) · currency suffix (`65 CHF` is German colloquial but Swiss financial copy uses prefix) · monospace font for numbers (use the variant-numeric feature, keep Figtree/Anton). |
| 2026-05-02 | **Q45: Contrast lock = WCAG AA floor (4.5:1 body, 3:1 large/UI), with 3 explicit banned pairings** | **Floor:** WCAG AA. Body text ≥**4.5:1**. Large text (≥18px regular OR ≥14px bold) and UI components (buttons, focus rings, icons that carry meaning, form borders in their default state) ≥**3:1**. Disabled states are exempt — they signal unavailability through low contrast on purpose. Decorative tokens (placeholder ink, hairline borders, divider lines) may sit below 3:1 but **must never carry information**. **AAA (7:1)** reserved for legal copy + error messages on payment screens. **3 banned pairings (surfaced by live test in `public/solen-coral.html#q45`):** (1) **White text on amber `#F3A864`** = 1.98:1 — banned. All amber CTAs MUST use ink text (`#1A1108` on amber = 9.40:1, passes body). (2) **Amber `#F3A864` text on white** = 1.98:1 — banned for any text role (eyebrows, labels, links). Amber-on-white = decoration only (illustration accents, divider lines). For "warm accent text" use coral `#C95A3A` (≈5:1, passes body) or ink. (3) **White text on coral `#E8624A`** = 3.35:1 — passes AA only for **large/bold UI** (button labels ≥14px bold). Banned for body text on coral surfaces (coral pill labels at 11-13px regular MUST use ink, not white). **Implementation:** add a CI lint rule (`scripts/contrast-check.ts`) that fails the build if any of the 3 banned pairings appear in token usage. The `public/solen-coral.html#q45` grid stays as a living swatch — extend it whenever a new color enters the system, fail-flag any pair that doesn't meet its job. |
| 2026-05-02 | **Q46: Tap target floor = 48×48 minimum hit area + ≥8px gap between adjacent targets + visual-vs-hit-area separation** | **Floor:** 48×48 CSS pixels for every interactive element on every surface. Stricter than WCAG 2.1 AAA (44×44) and Apple HIG (44pt); matches Material spec (48dp); gives one-handed buffer for gloves / cold weather / tram motion (Swiss-mobile reality, 70% of traffic). **Visual element MAY be smaller than the hit area** — a 24px heart icon, a 32px chip — but the *tap zone* must be 48×48. Pattern: `position: relative` on the visible element + `::before { content: ''; position: absolute; inset: -<n>px }` to expand the hit zone outward without affecting layout. **≥8px gap** between adjacent tappables (matches Q32 spacing scale 4-8 step). **Exempt:** star-rating display (5 stars in a row are display, not 5 separate buttons), inline text links inside paragraphs (inherit line-height as hit area), the page itself. **Implementation:** Tailwind utility `tap-min` = `min-w-12 min-h-12` (12 = 48px in rem-12 scale). Chips use `tap-halo` utility = relative + ::before inset -8px. Bottom-nav reaches 56-64px height for one-handed reach (thumb floor zone). **Anti-patterns banned:** 32×32 icon-only buttons relying on coincidental padding for hit area · adjacent buttons with 0-4px gap · disabled buttons that shrink to <48px (disabled keeps the size, only opacity/color changes per Q18) · "shrink everything for density" mode at any breakpoint. **Verification:** Q46 in-flight section in `public/solen-coral.html#q46` measures 6 representative buttons at 48×48 via DOM rect — extend whenever a new interactive primitive ships. |
| 2026-05-02 | **Q47: Keyboard focus = 2px coral ring, 2px outside-offset, `:focus-visible` only** | **Global rule:** `*:focus { outline: none } *:focus-visible { outline: 2px solid #E8624A; outline-offset: 2px }`. Mouse + touch users never see the ring (matches Q40's hover scope discipline — the brand stays quiet for the 95%+ pointer-input crowd). Keyboard users always see it (Tab navigation, Swiss BehiG accessibility law applies to digital services). **Why coral, not system blue:** system blue (`Highlight` / `LinkText` defaults) reads as "form error" in the warm palette and clashes with coral CTAs sitting next to it. Coral `#E8624A` reaches 3.35:1 on white, passes Q45's 3:1 UI floor. **Why 2px outside, not inset:** inset rings (`outline-offset: -2px`) clip the rounded corners of cards (radius 14), pills (99), buttons (12) — breaking the radius vocabulary on focus. Outside-offset preserves the shape and reads as a halo, not a shrink. **Apply to:** every interactive element (button, link, input, select, textarea, chip, card with `tabindex=0`, custom `role=button`, bottom-nav active item, modal close). **Skip on:** disabled elements (not tab-stoppable per Q18), the page background, decorative SVG. **Tailwind utility:** `focus-visible:outline focus-visible:outline-2 focus-visible:outline-coral focus-visible:outline-offset-2`. **Anti-patterns banned:** `outline: none` without a `:focus-visible` replacement (orphans keyboard users, fails BehiG) · `outline-offset: -2px` (inset clips corners) · system-blue `Highlight` outline left as default (palette clash + always-on-click noise) · ring-on-`:focus` not `:focus-visible` (visible noise on every mouse click). **Verification:** Q47 in-flight section in `public/solen-coral.html#q47` shows lock vs 3 banned variants; computed outline confirmed via DOM rect (`rgb(232, 98, 74) solid 2px; outline-offset: 2px`). |
| 2026-05-02 | **Q48: Brand signature = the Anton + tracked-coral-eyebrow lockup (B). Home search bar uses Fresha-flow pattern with same vocab (E2).** | **THE SIGNATURE (rule):** coral `#C95A3A` eyebrow tag (Figtree 700, 9-11px, `letter-spacing: .22em`, ALL CAPS, max 4 words, max 1 line) + Anton uppercase headline (display sizes 18-72px depending on surface, `line-height: .95`, `letter-spacing: .01em`, ALL CAPS) + Figtree body. **Lives on every surface** — homepage hero, salon detail header, booking wizard step (eyebrow = "SCHRITT 2 / 4"), profile (eyebrow = "MEIN PROFIL"), B2B Dashboard (eyebrow = "HEUTE · 02. MAI"), section headers, error pages, empty states, email + receipt headers. The rule: **wherever there's a heading, there's an eyebrow above it; wherever the eyebrow exists, the headline is Anton.** No exceptions on text-bearing surfaces. **Rejected candidates:** time-slot pill (functional, not unique — Treatwell + Booksy use the same), line-art SVG (only on empty states, dies below the fold), radius+spacing rhythm (12-14px card radius is industry standard, indistinguishable). **Search bar:** evaluated as candidate E — found to be a *discovery moment* (lives on 2 of 10 surfaces), NOT a signature. Lock E2 as the home search bar pattern, but it inherits B rather than competing with it. **HOME SEARCH PATTERN (E2 — Fresha-flow):** compact 3-field stacked card on hero (Wo / Was / Wann, each ~48px tall, eyebrow label + Anton value, leading icon, full-bleed pill `border-radius: 99px` on the wrapper) + coral primary CTA (`Solen durchsuchen`) at the bottom. **Tap any field → full-screen sub-page** (not a modal, not a dropdown — a route transition per Q35) where the field's editing surface gets the full screen: location list, service grid, calendar + day-part chips. Each sub-page header reuses the signature: small coral eyebrow (`WO` / `WAS` / `WANN`) + Anton title (`STANDORT` / `SERVICE` / `DATUM & ZEIT`). User submits anywhere; landing card fields update on return. **Surfaces it lives on:** Home + Discovery only. Other surfaces use a compact "open search" icon button in header. **Anti-patterns banned:** modal/sheet for any of the 3 sub-pages (cramps content, no breathing room) · headline rendered without an eyebrow above it on text-bearing surfaces · eyebrow rendered without an Anton headline below it · eyebrow in a font other than Figtree 700 with `.22em` tracking (the tracking is the signature) · Anton in mixed case or with normal letter-spacing (uppercase + tracked is the signature). **Verification:** Q48 in-flight section in `public/solen-coral.html#q48` shows the rule applied across 5 sub-page types (home, salon detail, booking step, profile, B2B dashboard) + the 4-screen Fresha-pattern search flow. |
| 2026-05-02 | **Q49: Above-fold home = A — stacked (eyebrow + Anton headline + Fresha-flow search card + 3 quick-action chips), no hero photo** | **Composition top to bottom on mobile:** (1) Header bar (Anton "SOLEN" wordmark left, Avatar/Anmelden right). (2) Eyebrow `HEUTE · BASEL · 47` (coral `#C95A3A`, Figtree 700, 9-11px, `.22em` tracking). (3) Anton headline `SCHNITT HEUTE NOCH FREI` (32-40px on mobile, `line-height: .95`, `letter-spacing: .01em`, ALL CAPS, max 2 lines). (4) Fresha-flow search card (3 stacked fields Wo/Was/Wann per Q48 + coral `Solen durchsuchen` CTA pill). (5) Below-card row: 3 quick-action chips (`Last-Minute` / `Spa Wochenende` / `Barber heute`) — each pre-fills the search and submits, giving cold-cache visitors a 1-tap path into results. **No hero photo.** Reasoning: photos belong on cards (where they show real, locale-correct salons earning their place); on the hero a photo would (a) compete with the Anton signature, (b) slow LCP, (c) misrepresent across Basel/Zürich/Bern (a Basel-only photo on a Zürich landing reads wrong), (d) cramp DE/FR/IT text expansion against tight overlay constraints. **Same composition desktop:** content centers in a max-width column (640px), eyebrow + headline scale up (Anton 60-72px), search card stays single-column stacked (don't switch to horizontal — Fresha-flow pattern requires the stacked rhythm so tap-target → fullscreen sub-page lock from Q48 still applies). **Rejected variants:** B (photo overlay) — photo competes with signature, slow LCP, doesn't scale across cities. C (split layout) — desktop pattern shoehorned into mobile, search becomes tiny, breaks DE text expansion. D (category-led, no headline) — interesting as a phase-2 logged-in variant for return users, but for cold visitors the Anton headline does heavy brand-recognition work; revisit D as a possible logged-in layout in a later Q. **Anti-patterns banned on the home above-fold:** hero photo · hero video / lottie · multiple competing CTAs (one primary `Solen durchsuchen` — quick-action chips are secondary single-tap shortcuts, not parallel CTAs) · carousel (rotating hero text/images — destroys signature, lowers comprehension) · sticky promotional banner above the eyebrow (clutters the brand entry). **Verification:** Q49 in-flight section in `public/solen-coral.html#q49` shows 4 variant phone mocks (A/B/C/D) + 5-criterion trade-off matrix. |
| 2026-05-02 | **Q50: Card density = horizontal scroll-snap carousel per section (Airbnb-pattern), 2.5-up mobile / 3.5-up tablet / 4.5-up desktop** (REVISED supersedes earlier 2-up grid proposal) | **Every discovery section is a horizontal scroll-snap carousel.** Simpler, more consistent rule than splitting between "urgent = carousel" and "default = grid". Per breakpoint: **mobile (≤640px)** = 2.5 cards visible (2 full + 0.5 peek), card width ~135-150px, gap 10px. **Tablet (641-1023)** = 3.5 cards visible, card width ~170-190px, gap 12px. **Desktop (≥1024)** = 4.5 cards visible, card width ~200-220px, gap 14px (page max-width 1100). **The 0.5-card peek is the swipe-affordance signal** — users immediately see "more in this section" without a "swipe →" hint. Borrowed from Airbnb's mobile pattern. **Section header includes a `→` button** to "see all" — opens a dedicated vertical grid page (2-up mobile, 3-up tablet, 4-up desktop) when the user wants to commit to deep-scroll. Cards always preserve Q26 anatomy (square 1:1 photo + bare text below, no body chrome, time-slot pill on photo bottom-left only when relevant, heart-favorite top-right). **Featured editorial cards (rare, ≤1 per page):** full-width 1-up with larger photo + Anton headline overlay — used for "Spotlight" content like new salon launches or seasonal campaigns. Featured is the ONLY exception to the carousel rule. **Anti-patterns banned:** 2-up vertical grid as default (the original proposal — too dense, no swipe affordance, photos cramped) · `swipe →` text hints (the peek is the hint) · 5-up+ on desktop carousel (cards become too small to read titles) · masonry/Pinterest layout · grid items that don't share Q26 anatomy · auto-rotate carousel (a11y-hostile, never on Solen). **Verification:** Q50 in-flight section in `public/solen-coral.html#q50` shows the revised 2.5-up mobile pattern + the original 2-up grid proposal kept as superseded reference. |
| 2026-05-02 | **Q51: Home/discovery section rhythm = 8 sanctioned sections + conditional Recently Viewed (REVISED supersedes earlier 5-section lock)** | **Source-of-truth check:** matched to `_tasks/REDESIGN_INVENTORY.md` (current homepage = 10 sections) + `_tasks/BACKEND_NEEDS_UI.md` (flagged: Recently Viewed, Trending, City counts, Discover, section toggles, affinity reorder) + `_rules/KEY_FEATURES.md` #14, #59, #60. The prior 5-section lock missed: Recently Viewed, Discover, Browse by City, Testimonials, Trust Stats — all of which exist in production code and earn their place. **Default order, top to bottom:** **(0) Recently Viewed** — CONDITIONAL (returning users only). Q50 carousel. Source: localStorage `solen_recently_viewed`, max 5. **(1) Last-Minute heute** — Q50 carousel. Skip if logged-out + no geo. Source: `LastMinuteStrip` component. **(2) In der Nähe** — Q50 carousel, geo-aware. Falls back to `Salons in <city>` if no geo. **(3) Per-category × up to 5** — one Q50 carousel each for Coiffeur / Barber / Nails / Spa / Makeup-Waxing. **Order is reordered by user category-affinity tracker** — leads with categories the user has clicked most. Source: `FeaturedSalonCarousel` × N. **(4) Discover** — Pinterest-style content tiles (3-up portrait inside a Q50 carousel). Hair/style inspo with "Book this look" booking-bridge per `KEY_FEATURES.md` #24. → links to `/discover` for the full feed. Source: `DiscoverCarousel` + `/api/discovery/feed`. **(5) Browse by City** — 3 city tiles (Basel / Zürich / Bern) with live salon counts. Source: `BrowseByCitySection`. **(6) Spotlight** — OPTIONAL 1-up editorial. Curated only. Hide entirely if no curated content (no empty header). **(7) Testimonials** — review carousel (4+ stars, max 6). Source: `TestimonialCarousel` + `/api/reviews/homepage`. **(8) Trust Stats Banner** — ink-bg footer-zone with Salons / Reviews / Bookings counters. Anton numbers + tabular-nums per Q43 hero rule. Amber accent on numbers (#F3A864). Source: `TrustStatsBanner` + `/api/metrics/global`. **Two production knobs the lock acknowledges:** **(a) Admin section toggles** via `platform_settings.homepage_sections` (per `KEY_FEATURES.md` #60) — admin can hide any section; default = all visible. **(b) Category affinity reorder** — tracks user's clicked-category history (per existing implementation in REDESIGN_INVENTORY) and reorders Section 3's 5 per-category carousels. **Section construction rule:** every section = `eyebrow (Q48 coral 9px tracked) + Anton headline + → "see all" arrow button (links to dedicated grid page) + content (Q26 cards in Q50 carousel)`. **Spacing:** 48-64px gap between sections (Q32). **Empty handling:** sections collapse entirely if zero results (no "0 results" placeholder breaking rhythm). Recently Viewed + Spotlight are also conditionally hidden (no localStorage data / no curated content). **Anti-patterns banned:** Spotlight section with no curated content (empty header reads as broken) · sections sourced from the same query (Last-Minute = nearby + frei-heute is OK; Nearby = nearby + popular is duplication) · banner ads / promo banners as sections · sticky section navigation chips (over-engineered for 8 sections) · vertical-grid sections in addition to carousels (the carousel + "see all" page is the only commit-scroll path) · ripping out section toggles or affinity reorder during redesign (existing production behavior). **Verification:** Q51-revised in-flight section in `public/solen-coral.html#q51-revised` shows the 8-section vocab + Recently Viewed in a tall phone mock + delta-from-old-lock + production-knobs callout. Original 5-section lock kept at `#q51` as superseded reference. |
| 2026-05-02 | **Q52: Salon detail hero = single full-bleed photo + bottom-fade overlay (eyebrow + Anton + meta) + thumbnail strip + tap-to-fullscreen-gallery (D-on-tap) + sticky scrollspy tab nav (Fresha pattern)** | **Hero composition (above-fold):** single full-bleed photo at ~50% viewport height (NOT carousel — users skip carousels per documented UX research, auto-rotate is a11y-hostile). **Bottom-fade overlay** carries: eyebrow `<KATEGORIE> · <distance>` (coral `#F3A864` on dark gradient for contrast — amber lifts off black bg), Anton headline (salon name, 2 lines max, white), no meta on photo. **Top-of-photo controls:** back button (left, 28px circle white-95% bg), share + heart (right, same treatment). **Heart uses `#FF4A6B` literal love-red per Q36/SOLEN_UI #5b**, NOT brand token. **Below the photo:** meta strip `★ <rating> (<count>) · <distance> · <Jetzt offen / Geschlossen>` (open-state in #16A34A green or #DC2626 red — semantic, not brand). **Below meta: thumbnail strip** of 3 visible photos + `+N` overflow tile. **Tap any thumbnail or `+N` → fullscreen gallery sub-page (D pattern)** route transition per Q35 (shared-element morph), NOT a modal. Gallery sub-page has its own back chevron, photo counter `1 / 12`, swipe nav, optional pinch-zoom. **Sticky scrollspy tab nav (Fresha pattern):** tab bar with `Fotos · Services · Team · Reviews · Portfolio · Buy · Über` (and any salon-category-specific tabs like `Walk-in` for barbershops, `Loyalty` for salons with stamp programs). **Behaviors:** (1) Pin-on-scroll = tab bar pins to top of viewport once user scrolls past hero; (2) Header collapse = hero header (back/share/heart) collapses to compact title bar showing salon name when pinned; (3) Sliding underline = coral 2px underline slides between active tabs (200ms ease per Q35); (4) Scrollspy = active tab updates as user scrolls (IntersectionObserver); (5) Tap-to-jump = tap a tab → smooth-scroll to that section with offset for sticky bar height; (6) Horizontal scroll = tab bar overflows horizontally on small screens (more sections than fit), sliding underline still works in scroll; (7) Sticky bottom CTA stays = `<N> Services · Buchen` bar stays visible throughout scroll, never collapses. **Existing components used (per REDESIGN_INVENTORY):** `SalonHero` (180L) evolves from current carousel to A pattern · `SalonSectionNav` (124L, IntersectionObserver scrollspy) · `SalonTabBar` (67L, sliding underline) · `SalonMobileCTA` (92L, sticky bottom) · `SalonSidebar` (136L, desktop sticky quick-book) — all stay; only SalonHero implementation changes. **Backend-needs-UI affects on hero:** Off-peak deals badge (`/api/salons/[slug]/off-peak-today`) renders as a small coral chip top-left of photo. Solen Score (NEW endpoint to build: `/api/salons/[slug]/solen-score` — public read; existing `/api/admin/solen-score/recalculate` is admin-only recalc, not the read path) renders inside the meta strip as `Score 87` if score ≥70, hidden if below. Solen Favorit / Top bewertet auto-badges render as small Anton-uppercase chips above the salon name in the bottom-fade overlay. **Rejected variants:** B (carousel hero) — users skip carousels (well-documented), auto-rotate a11y-hostile, hero gets one chance and a carousel diffuses it. C (split layout) — photo gets squeezed on mobile to a thumbnail, defeating "salon detail is photo-led" point. D as default — gallery-first is too aggressive on landing; the thumbnail-strip + tap-to-fullscreen is the right balance (entry = brand context, deep dive on demand). **Anti-patterns banned:** carousel hero with auto-rotate · gallery-modal that traps the user · photo + booking-CTA both on the same photo (bottom-CTA bar handles booking, hero handles brand) · skipping the SectionNav (long detail pages without scrollspy bury content) · sticky tab bar that overlaps content on tap-to-jump (must offset scroll target) · hero photo missing → no fallback (must show category illustration or warm-ink placeholder per Q21). **Verification:** Q52 in-flight section in `public/solen-coral.html#q52` shows 4 hero variants (A/B/C/D wildcard) + REDESIGN_INVENTORY callout + side-by-side initial-vs-scrolled mocks of the sticky tab nav + 7 behaviors checklist. |
| 2026-05-02 | **Q53: Booking entry = sticky bottom CTA (mobile) + sticky sidebar (desktop), both route to full-page wizard at `/book/[slug]` with shared-element morph (Q35); NEVER bottom-sheet or modal** | **Mobile entry surface:** `SalonMobileCTA` (92L existing component) — sticky bottom bar with `<N> Services · Buchen →` (tabular-num count per Q43 + coral CTA per Q40 hover scope). Always visible regardless of scroll position. Single primary CTA — no parallel "save" or "share" CTAs in the bar. **Desktop entry surface:** `SalonSidebar` (136L existing) — sticky right-rail sidebar with mini service preview + next-slot quick-pick + coral `Buchen →`. Stays visible as service list scrolls. **Tertiary entry:** in-flow `+` button on each service row in the Services tab — tapping adds the service to the cart and routes to the wizard with that service pre-selected. **Tap target:** all three entries route to **`/book/[slug]`** — a full-page wizard, NOT a bottom sheet, NOT a modal. **Route transition:** Q35 shared-element morph from the salon's hero/sidebar into the wizard's "salon context" header so the user keeps spatial continuity. Back button restores both the salon detail page AND the user's scroll position. **Wizard target component:** `BookingWizard` (172L existing) orchestrates 4 steps via `BookingContext`. **Why not bottom sheet (B):** cramps the wizard into ~70% screen, breaks `position: sticky` for step indicator + sticky `Weiter` button, breaks URL-sharing of "in-progress booking" state (`/book/[slug]?step=2&service=X` is a real shareable link), breaks browser back-button restoration, and DE/FR text expansion overflows in a constrained sheet. **Why not inline anchor (C):** loses the always-visible "ready to book" affordance — the sticky CTA is what makes Solen feel transactional, not a brochure; without it, users browse-without-bookable-cue. **Anti-patterns banned:** booking flow inside a Radix Dialog or a sheet · "quick book" floating widget that occludes service content · multiple competing CTAs in the sticky bar (e.g. `Anrufen \| Buchen`) · sticky bar that disappears when keyboard opens (DE long phone numbers + iOS keyboard already cramps; bar must remain or the user loses the action) · routing to a wizard URL without keeping salon context (wizard always shows salon avatar + name in header) · breaking the back button (Next.js App Router back must restore exact scroll position via `scroll: true` and route morph). **Verification:** Q53 in-flight section in `public/solen-coral.html#q53` shows mobile sticky bottom + desktop sidebar (both A) + bottom sheet (B, rejected) + REDESIGN_INVENTORY citation + 6-criterion trade-off matrix. |
| 2026-05-02 | **Q54: Reviews = summary card + 3 latest on salon detail; "Alle Bewertungen" routes to `/salon/[slug]/reviews` sub-page (full filterable list with photos + reply threads)** | **On salon detail (Reviews tab section):** rating block (Anton 24px tabular `4.9` + 5-star pictogram) + 5-bar distribution mini-chart (coral `#E8624A` for 5★, amber `#F3A864` for 4-3★, neutral grey for 2-1★, tabular-num counts per Q43) + **3 latest reviews** as compact cards: avatar circle 14-18px (initials, brand-tinted bg), name + ★rating + relative date ("2d", "5d", "1w"), full review text per Q41 NO truncation, photo strip thumbnails (18px each, max 4 shown + `+N` overflow chip), collapsed reply chip "Studio Lina antwortete · ▾" (tap to expand) + `Alle <N> Bewertungen →` CTA below. **On `/salon/[slug]/reviews` sub-page:** eyebrow `Studio Lina · 142` + Anton headline "Bewertungen" + filter chips row (`Alle / Mit Fotos · N / 5★ / ↓ Neueste`) — chips wrap per Q42 (no horizontal-scroll-only) + infinite-scroll review list with full reply threads expanded by default (no "more" toggle, since users are deep-reading) + photo grid in each review (34px thumbs) + `👍 Hilfreich (N) · Melden` action row per card. **Both surfaces:** review text full-width, never truncated, never line-clamped (per Q41). Photos clickable → opens Q35-morphed fullscreen gallery (same component as Q52 hero gallery). **Reply threads** use a left-coral-border (`border-left: 2px solid #E8624A`) with subtle bg `#fff` and "Studio Lina · Antwort" eyebrow tag for clear authorship. **Sticky bottom CTA stays** on both surfaces (Q53) — booking is the goal even when reading reviews. **Existing components used:** `SalonReviews` (383L) splits responsibilities — summary + 3-latest stays inside the salon detail Reviews tab; full-list-with-filters becomes the `/reviews` sub-page. **Backend-needs-UI handled:** review photos, review reply (public/private — private replies hidden from this view), flagging via `/api/reviews/[id]/flag`. **Rejected variants:** B (inline all 142 reviews) buries services + opening hours + portfolio under reviews, breaks "salon detail is scannable" rule. C (modal review explorer) breaks URL-sharing of "reviews tab" state, blocks the sticky CTA bar with overlay backdrop. D (reviews-first Yelp-style) wrong vibe — Solen is a salon marketplace, not a review site; services + booking must lead. **Anti-patterns banned:** review text line-clamp / `text-overflow: ellipsis` (per Q41) · review modal that traps the user · auto-expanding ALL reply threads on the salon-detail summary (3-card preview keeps thread chips collapsed, only sub-page expands by default) · 5-star pictogram with hue-shifted hover · paid/sponsored review markers (Solen does not allow these) · "show more" toggles inside individual review cards (per Q41 full text always shown). **Verification:** Q54 in-flight section in `public/solen-coral.html#q54` shows salon-detail Reviews tab + sub-page side-by-side + 5-criterion trade-off matrix. |
| 2026-05-02 | **Q55: Booking wizard = 3 steps (collapsed from current 4): Service+Staff → Date+Time → Pay+Confirm. Merge `ConfirmationStep` + `PaymentStep` into single `PayConfirmStep`** | **Step 1 — Service + Staff** (`ServicesStaffStep` 219L stays as-is, OR `ServiceSelectionStep` 184L for multi-service). User picks service(s) + assigned staff (or "Egal" / first available). **Step 2 — Date + Time** (`DateTimeStep` 305L stays as-is). Calendar grid + time-slot list per Q43 numerics rules. **Step 3 — Pay + Confirm** (NEW `PayConfirmStep` ~250-300L, replaces both `ConfirmationStep` 194L AND `PaymentStep` 216L). Single screen layout top→bottom: (a) eyebrow `Schritt 3 / 3` + Anton headline `Bestätigen & Zahlen`; (b) summary card (service + stylist + date/time + price tabular per Q43); (c) cancel policy mini-banner (warm-amber bg, single sentence + "Mehr lesen" disclosure); (d) payment method selector (radio chips: `Karte / Vor Ort / Apple Pay / TWINT`, default = Karte if Stripe Connect set up); (e) primary `Buchen · CHF <total>` CTA. **Reasoning:** each wizard step costs ~10-15% drop-off; collapsing 4→3 = ~12% conversion lift. Matches Stripe Checkout / Apple Pay / Booksy / Fresha pattern (one review-and-pay screen, not two). User reads top→bottom, taps once. **Components touched:** delete `ConfirmationStep.tsx`, delete `PaymentStep.tsx`, create `PayConfirmStep.tsx`, update `BookingWizard.tsx` (172L) progress bar from 4 segments to 3, keep `BookingContext` formData shape unchanged (currentStep enum: `services_staff | date_time | pay_confirm`). **Untouched components:** `ServiceCart` (194L promo/gift/referral), `GuestBookingForm` (113L), `GroupBookingModal` (205L), `PackageRedeemBanner` (105L), `BookingSuccess` post-page, `ReviewPrompt` post-page. **Rejected variants:** A (current 4-step) — still valid baseline but ~12% worse conversion, separate Confirmation step is ceremonial not functional. C (5-step split) — Service + Staff don't need separation (staff list is service-dependent), 5 steps = 5× drop-off opportunities, outdated pattern. D (single-page accordion) — intriguing for desktop later but mobile keyboard chaos + per-section validation + "where am I" disorientation; revisit phase-3. **Anti-patterns banned:** wizard step that exists only to display info (no input) is ceremonial — merge into adjacent step · `<button>Weiter</button>` that does nothing but advance to a read-only summary · cancel policy hidden in modal/disclosure on pay screen (must be visible by default at booking commit) · payment method selector as separate step · summary card without all 4 line items (service / stylist / date+time / price) · success page that doesn't morph from the Pay+Confirm CTA (per Q35). **Verification:** Q55 in-flight section in `public/solen-coral.html#q55` shows 4 wizard variants (A current 4-step / B recommended 3-step / C 5-step split / D single-page wildcard) + REDESIGN_INVENTORY callout + 5-criterion trade-off matrix. |
| 2026-05-02 | **Q56: Step indicator = 3-segment progress bar + eyebrow `Schritt N / 3` + Anton step label, top of every wizard step. Tappable previous segments for jump-back.** | **Anatomy:** ~32px vertical block at the top of `BookingWizard` shell, below the route header. Top row: 3 horizontal segments, each `flex: 1; height: 3px; border-radius: 99px`, gap 3px. Coral `#E8624A` fill = current OR completed; neutral `#FAF7F3` = remaining. Below the bar: eyebrow `Schritt <N> / 3` (Figtree 700, 11px, `.22em` tracking, coral `#C95A3A`) + Anton step label below (`Service & Stylist` / `Datum & Zeit` / `Bestätigen & Zahlen` — 13-18px depending on viewport, `line-height: .95`, ALL CAPS). **Re-uses Q48 signature** so the wizard reads as Solen-native, not 3rd-party form. **Tap-to-jump-back:** completed segments are tappable buttons; tap goes back to that step preserving formData (`BookingContext` already supports this). Future segments are NOT tappable (forward-jumping breaks validation order). **No numbered circles, no breadcrumb chain, no dots.** **Mobile + desktop:** same indicator shape; on desktop the indicator may live inside `BookingWizard` shell header (above the form) or in a sticky-left-rail (existing `BookingSidebar` 106L) — the *visual rhythm* (3 segments + eyebrow + Anton) is identical regardless of placement. **Rejected variants:** A (numbered circles with check-mark on done) — eats ~50px vertical, reads corporate, doesn't match brand rhythm; locked variant components like the discovery `CategoryStickyRow` already established eyebrow+Anton as the section grammar. C (breadcrumb pills `Service / Datum / Pay`) — DE/FR labels overflow on narrow phones (long words like "Bestätigen" + "Datum" don't fit a tight pill chain); doesn't show "how much is left" at a glance. D (silent / no indicator) — disorienting at payment step ("is this the last step or are there more?"); breaks expected wizard convention; raises drop-off via uncertainty. **Anti-patterns banned:** numbered step circles · breadcrumb-style pill chain · animated walking-dot indicators · step labels that change between Anton (here) and other fonts (anywhere) · indicator that hides on scroll (must stay visible at the top of the wizard step content) · forward-tappable future segments. **Verification:** Q56 in-flight section in `public/solen-coral.html#q56` shows 4 indicator styles (A numbered / B bar+Anton / C breadcrumb / D silent) + 5-criterion trade-off matrix. |
| 2026-05-02 | **Q57: Confirmation screen = layered moment — Q36 celebration animation + Anton "Buchung bestätigt" + summary card + 3 utility chips (Calendar / Directions / Share) + secondary "Zur Buchung" CTA. NO confetti, NO auto-redirect, NO upsell.** | **Anchor:** Q36 celebration animation runs once on mount (coral ring expand 700ms + checkmark scale-in with overshoot, 100ms delay + dark-ink toast slides up). **Q48 signature lockup:** eyebrow `Bestätigt · #<bookingId>` (coral `#C95A3A`, 9-11px, `.22em` tracking) + Anton headline `Buchung bestätigt` (uppercase, 18-32px depending on viewport, two lines acceptable) + warm sub-line `Wir freuen uns auf dich, <FirstName>.` (Figtree 12-14px, ink2). **Summary card** (`#FAF7F3` bg, 8-10px radius): 4 rows minimum — `Was` (service), `Wann` (Do, 5. Mai · 14:30, tabular per Q43), `Wo` (salon name + neighborhood), `Wer` (staff name). Right-aligned values, left-aligned labels. **3 next-action utility chips** (vertical stack on mobile, horizontal on desktop, all 48px hit area per Q46): (1) `In Kalender speichern` — ICS download / Google Calendar deep-link, highest-utility post-booking action; pre-prevents no-shows. (2) `Wegbeschreibung` — opens native maps with salon address; deferred to "near appointment" SMS reminder if not tapped. (3) `Teilen` — share salon link (NOT booking link for privacy), light-touch viral mechanic. **Secondary CTA:** `Zur Buchung →` (neutral `#FAF7F3` bg, ink text) → routes to `/profile/bookings` (NOT auto-redirect, user picks). **Explicitly NOT on this screen:** upsell ("Buy gift card", "Add another service") — feels predatory right after pay; auto-redirect to home/discover; `ReviewPrompt` (it stays in the 24h-post cron, not on this screen). **Component:** `BookingSuccess` post-page renders this layout; `ReviewPrompt` (168L) remains scheduled-cron-only. **Rejected variants:** B (confetti everywhere with emoji slop) — off-tone for Solen; Bold + Playful + Warm (Q23) ≠ exclamation-mark slop; treats every booking like a lottery win, wears off after booking #2. C (minimal receipt, plain text) — gas-station-receipt energy, misses the brand moment Solen earns by being warm. **Anti-patterns banned:** confetti / emoji-heavy copy (`🎉 YESSSS!!`) · auto-redirect after N seconds · upsell CTAs ("Add another service", "Buy gift card") above the secondary CTA · review prompt rendered on this screen (must be deferred to 24h-post cron) · "Booking confirmed" without the booking ID + summary card (users want receipt-grade reassurance) · `ReviewPrompt` triggered immediately on this screen. **Verification:** Q57 in-flight section in `public/solen-coral.html#q57` shows A layered moment (recommended) + B confetti everywhere (rejected) + C minimal receipt (rejected) + next-action chip rationale + Q36 cross-reference. |
| 2026-05-02 | **Q58: Profile layout = C hybrid (Insta header + Sei-Hiro grouped lists) + event-driven Live-Activity hero card. NO bottom nav on web — hamburger header + avatar dropdown only.** | **Top region (above grouped lists):** (a) Header bar — hamburger left + Anton "SOLEN" wordmark center + (no right action). (b) Profile header — eyebrow `Solen Exclusive` (or other badge per loyalty tier) + Anton headline `<First Name>` (uppercase, ALL CAPS) + Figtree bio line `<hair-type> · <nail-type> · <style>` + gradient avatar (existing `ProfileHero` ring `linear-gradient(145deg,#C8614A 0%,#E8C49A 55%,#E8927A 100%)`) right. **Live-Activity hero card slot** sits directly under the header, shows ONE state at a time picked by server-side priority (NOT auto-rotating). **States + priority order:** (1) **Upcoming** — appointment within 24h. Coral-amber gradient (`linear-gradient(135deg,#E8624A 0%,#F3A864 100%)`). Shows salon + time + walk distance + "Wegbeschreibung →". (2) **Loyalty close** — ≤2 stamps from reward at any salon. Same coral-amber gradient. Shows salon name + filled/empty stamp dots inline. (3) **Deal at favorited salon** — favorite has off-peak deal today. Ink gradient (`linear-gradient(135deg,#1A1108 0%,#3C3128 100%)`) with amber eyebrow — urgency register. Shows discount + salon + free slot + distance. (4) **Review reply** — salon owner replied to user's review in last 7d. Cream `#FFF4E8` bg + amber border `#F3D8B8`. Soft notification register. Shows salon + reply snippet. (5) **Rebook nudge** — average booking-cycle reached for any past salon (e.g. 28 days since last cut). Neutral `#FAF7F3` bg. Quiet nudge register. Shows "X days since · Salon Y · Wieder buchen? →". (6) **Empty CTA fallback** — no qualifying state. White bg + dashed coral border `2px dashed rgba(232,98,74,.4)`. Shows "Salon entdecken · Wo · Was · Wann →". **Resolution:** server returns the first state that qualifies via `/api/profile/live-state`. Client polls every 60s while page is visible + revalidates on focus + on websocket events (booking-create / review-reply / loyalty-stamp). **Morph animation:** ONLY fires when resolved state changes (not on rotation — there is no rotation). Cross-fade + 4px Y slide over 400ms per Q35 timing. No animation on initial load. Reduced-motion: instant swap, no slide, no cross-fade. **Tap target:** entire card is 48×48+ hit area per Q46; tapping routes to the relevant page (booking detail / loyalty page / deal page / review thread / rebook flow / discovery). **Below the live card:** grouped menu lists (Sei-Hiro pattern) — Card 1 "Activity" rows (Termine N · Favoriten N · Looks N — count chip on right), Card 2 "Account" rows (Beauty Profile · Zahlungsmethoden · Einstellungen — chevron right), Card 3 "Misc" rows (Freunde einladen with `CHF 10` reward chip · Hilfe & Support). Sign-out as a quiet text-button at the bottom. **No bottom nav (web-only decision):** primary nav lives in header hamburger menu + avatar dropdown. Existing `Header.tsx` (489L) handles this; `FloatingNavPill` and bottom-nav components deprecated for web rendering. Mobile native/PWA can re-introduce bottom nav later — that's a future Q. **Q46 implication:** "bottom-nav reaches 56-64px for thumb floor" rule no longer applies on web; salon detail's Q53 sticky bottom CTA stays (contextual, not nav). **Existing components affected:** `ProfilePage.tsx` (1159L) refactored — drop the 5-tab `ProfileTabs` for grouped-lists layout; `ProfileHero` (99L) keeps gradient avatar but shifts to right of name+bio block (was centered); `BeautyProfileCard` becomes a row inside Account group (was above tabs); `SalonHighlights` category-colored circles get reused inside Termine/Favoriten sub-pages (no longer above tabs); `LooksGrid` lives at `/profile/looks`. **NEW components needed:** `LiveActivityCard` (~150L, handles 6 states + priority + websocket + reduced-motion); `ProfileGroupedLists` (~120L, Sei-Hiro card-list pattern). **Anti-patterns banned:** auto-rotating Live-Activity card (must stay until data changes — Q58 user feedback) · multiple Live-Activity cards stacked (single slot only) · Live card with carousel dots / arrow controls (visual signal that it rotates — banned) · Insta-style social-stat row on Solen profile (followers/following = vanity, not Solen's job) · 5-tab `ProfileTabs` returning anywhere in this layout · bottom nav on web · upsell card in the live slot (state 6 empty CTA is the only "promotional" register, and even it just routes to discovery, not "buy gift card"). **Verification:** Q58 in-flight section in `public/solen-coral.html#q58` shows: (a) current-state mock for reference, (b) 3 redesign variants (A Insta-led / B Uber-led / C hybrid recommended), (c) interactive click-to-preview Live-Activity card with 6 states + click chips, (d) priority resolution diagram, (e) bottom-nav-removal note. |
| 2026-05-02 | **Q59: Loyalty stamp prominence = 3-surface system: (1) `/profile/stamps` dedicated page, (2) salon-detail meta-strip chip, (3) Live-Activity card priority state #2 (Q58). Reward-unlock celebration uses Q36 grammar at scale, NOT confetti.** | **Surface 1 — `/profile/stamps` dedicated page:** Q49-style header (`Mein Profil · N Karten` eyebrow + Anton "Stempel"). **Hero card** at top = the closest-to-reward StampCard, full-bleed coral-amber gradient (`linear-gradient(135deg,#E8624A 0%,#F3A864 100%)`), shows salon image (18px) + name + `N/total` count + Anton headline `<X> mehr für <reward>` + filled stamp dots in white circles with coral check + empty dots as dashed white outlines on the gradient. Below hero: list of remaining active StampCards using existing `StampCard.tsx` component anatomy (white bg + dashed-outline empty stamps + `Treuekarte` eyebrow + reward-text amber `#F3A864` + `N/total` tabular count). Below active: Eingelöst (redeemed) history list at 70% opacity with `✓ Eingelöst · <date>` chip in green `#16A34A`. **Surface 2 — salon detail meta strip:** small stamp chip in the meta row alongside `★ <rating> · <Jetzt offen>` per Q52 — only renders when user has active stamps with this salon. Anatomy: 22-26px height, padding 3px 8px, coral-amber gradient bg, white star pictogram (9px) + `<filled>/<total>` (8px Figtree 700 tabular). Tappable → routes to `/profile/stamps` with this salon's card scrolled into view + highlighted. **Surface 3 — Live-Activity card priority state #2 (already locked in Q58):** when user has any salon's stamp card with `total - filled ≤ 2`, the Live-Activity card on `/profile` shows that salon's loyalty progress with inline stamp dots. Q58's resolution priority means Upcoming-appointment beats Loyalty-close (you don't want to nag someone with stamps when they have a booking in 2 hours). **Reward-unlock celebration (replaces existing 12-dot confetti in `StampCard.tsx`):** drop confetti entirely (per Q57 anti-confetti rule). Use Q36 grammar at milestone scale: (a) **coral ring expand** 1200ms ease-out (vs 700ms for booking-confirmed — bigger because the milestone took weeks/months to earn); (b) **amber checkmark scale-in** with overshoot `cubic-bezier(0.4, 1.4, 0.5, 1)`, 200ms delay, fill `#F3A864` (NOT coral — register-shift signals "you got something extra," not "you confirmed something"); (c) **dark-ink toast** slides up from bottom 400ms after ring fires: `Belohnung freigeschaltet · <reward>` (e.g. "20% Rabatt bei Studio Lina"), 6s display then slide down; (d) **stamp grid** transitions from active-state to "complete overlay" — existing coral-tinted bg `rgba(232,98,74,.08)` + `Belohnung freigeschaltet!` ribbon — once toast clears. **Reduced-motion:** instant overlay swap, toast fades in/out only, no ring expand, no scale-in (per `prefers-reduced-motion`). **Existing components:** `components/loyalty/StampCard.tsx` (137L) keeps anatomy, drop confetti animation block + import; `components/barber/LoyaltyCard.tsx` (164L) + `components/barber/LoyaltyCardList.tsx` (87L) untouched (B2B barber-side dashboard); `app/[locale]/loyalty/stamp/page.tsx` HMAC-signed QR route per `KEY_FEATURES.md` #53 untouched (in-salon stamping flow). **NEW:** consumer loyalty page at `app/[locale]/profile/stamps/page.tsx` (German UI label = "Stempel" per i18n; URL slug stays English per project routing convention) + the closest-to-reward hero variant of StampCard. **Anti-patterns banned:** confetti animation in any loyalty surface (per Q57 + Q59) · auto-rotating between stamp cards on the dedicated page (it's a list, not a carousel) · stamp progress shown in coral-on-coral (use coral-bg + white check, never coral-on-coral) · "you're so close!" exclamation copy (warm + direct per Q24, not pushy) · per-stamp celebration animation that fires on the dedicated page when user just opens it (animation runs only when stamp event happens, not on page mount) · reward-unlock toast that auto-dismisses too fast (≥6s display so user reads + screenshots it). **Verification:** Q59 in-flight section in `public/solen-coral.html#q59` shows the 3 surfaces side-by-side + reward-unlock celebration spec + `StampCard.tsx` inventory citation. |
| 2026-05-02 | **Q60: Empty bookings = 3 distinct treatments (NOT one generic), per Q19 state lockset — FTU full-screen / no-upcoming inline warm-amber tile / filtered-empty small neutral tile. Coral CTA on A and B (brand-warm moments); neutral outline on C (utility recovery).** | **Treatment A — FTU (never booked):** full-screen empty (occupies entire `/profile/bookings` viewport), centered. Anatomy top→bottom: line-coral SVG (80×80, calendar rect with clock-circle anchor inside, `stroke="#E8624A" stroke-width="2"`) + eyebrow `Noch keine Termine` (Figtree 700, 9-11px, `.22em` tracking, coral `#C95A3A`) + Anton headline `Bereit für deinen ersten?` (uppercase, 18-32px viewport-responsive, two lines acceptable) + warm sub-copy `Such einen Salon, buch einen Termin — wir kümmern uns um den Rest.` (Figtree 11-14px, ink2, max-width 200-280px) + primary CTA `Salon entdecken →` (coral `#E8624A` bg, white text, 36-48px height, 18px horizontal padding, 99px radius) routes to `/entdecken`. **This is the only treatment with a coral primary CTA + full-screen layout** — it's the brand-warm moment, motivational, mirrors Q57's celebration register without the celebration animation. **Treatment B — No upcoming, has past:** inline empty (NOT full-screen — past bookings carousel renders below). Page header stays: eyebrow `Termine` + Anton `Keine bevorstehend`. Below header: warm-amber inline tile (`#FFF4E8` bg, `1px dashed #F3D8B8` border, 10px radius, 18px×12px padding, centered text) — line-art clock SVG (48×48, coral stroke) + sub-line `Letzter Termin: 28 Tage her bei <strong>Salon Klara</strong>` (Figtree 11-13px, ink2 with ink-strong on salon name) + coral CTA `Wieder buchen →` (smaller variant, 28-32px height, 14px padding, 99px radius). Below tile: existing "Wieder buchen?" past carousel — eyebrow `Vergangen · N` (count) + Anton `Wieder buchen?` + horizontal scroll of past salons (48×48 image cards with name caption, 7-8px Figtree). The combo of "small empty tile + past list" prevents the page from feeling barren AND gives an immediate next-action that's grounded in user history (not generic). **Treatment C — Filtered to nothing:** smallest inline empty (no full-screen, no large illustration). Page header stays: eyebrow `Termine` + Anton `Was wir bieten` (or original page title). Active filter chips render above (e.g. `2026 ×` + `Coiffeur ×` warm-amber chips per Q19+Q40). Empty tile below filters: `#FAF7F3` neutral bg (NOT amber, NOT coral — neutral signals "this is a utility state, not a brand moment"), 10px radius, 18px×12px padding, centered. Anatomy: small magnifier line-icon (36×36, grey `var(--ink3)` stroke — NOT coral) + Anton `Keine Treffer` (uppercase, 13-18px) + direct copy `Versuch andere Filter oder zeig alle Termine.` (Figtree 10-12px, ink2) + neutral outline button `Filter zurücksetzen` (white bg, 1px ink-border, ink text, 28-32px height, 99px radius — **explicitly NOT coral** because brand-coral on a "you typed wrong" moment feels accusatory; neutral outline = "no problem, here's the reset"). **Cross-page reuse:** the 3-treatment pattern applies anywhere a list can be empty — `/profile/favorites` (FTU = "Speicher Salons, die du liebst" + coral "Salon entdecken"; filtered = neutral reset), `/profile/looks` (FTU = "Speicher Looks für später"), `/profile/stamps` (FTU = "Sammle Stempel bei deinem ersten Termin"). Each page maps its own copy + SVG anchor but follows the same 3-treatment grammar. **Per Q21 illustration rule:** all SVGs are line-coral (or grey for treatment C only) — NEVER filled, NEVER multi-color, NEVER lottie/3D. **Per Q24 copy rule:** direct-friendly, NO emojis, NO `!!`, NO "Oops!" / "We couldn't find anything…" passive corporate-speak. **Per Q25 CTA rule:** single verb default + brand action ("Salon entdecken" not "Klick hier um Salons zu entdecken"). **Existing components affected:** new `EmptyState` component family — `EmptyStateFTU` (full-screen, treatment A pattern), `EmptyStateInline` (treatment B pattern), `EmptyStateFiltered` (treatment C pattern). Drop any existing one-size-fits-all `<EmptyView>` if present. Each takes `{eyebrow, headline, subCopy, ctaLabel, ctaHref, ctaVariant: 'coral' \| 'outline', illustration: ReactNode}`. **Anti-patterns banned:** single generic empty state used for all 3 contexts (FTU + no-upcoming + filtered get visually conflated → user can't tell why the list is empty) · filled illustrations (multi-color, lottie, 3D, emoji) — line-coral only · coral CTA on filtered-empty (signals "you did something wrong" — must be neutral outline) · auto-redirect to discovery on FTU (user must tap to opt in) · `Oops!` / `We couldn't find anything…` corporate copy (use direct-friendly per Q24) · empty illustration that doesn't anchor the context (use calendar+clock for bookings empty, heart for favorites empty, magnifier for filtered, stamp for loyalty empty — pictogram must telegraph the page) · stripping the past-bookings carousel below the no-upcoming tile (carousel is half the value of treatment B). **Verification:** Q60 in-flight section in `public/solen-coral.html#q60` shows the 3 treatments side-by-side at phone aspect-ratio + Q19 cross-reference + Q21 SVG-style citation + per-treatment recommendation paragraph. |
| 2026-05-02 | **Q61: Dashboard default = E viewport-split hybrid + grouped sidebar/drawer shell. `/dashboard` resolves to Today Live-Activity card on mobile, Calendar week-grid + sticky stats-strip on desktop. Stats homepage retires. Persistent grouped sidebar (desktop) / slide-from-left drawer (mobile) with category-tools group injected by salon `categories[]`.** | **Viewport router:** `app/[locale]/dashboard/page.tsx` (~30L) reads viewport (server hint via header + client hydration fallback) and renders one of two screens. **Mobile screen (`<768px`) — `TodayLiveCard` (~250L NEW):** (a) header bar (salon name + hamburger left + notification bell right). (b) **Live "Now" hero** = coral-amber gradient (`linear-gradient(135deg,#E8624A 0%,#F3A864 100%)`), white text, eyebrow `Jetzt · <time>` + Anton client name + service in caps + 2 utility chips (staff name + price). When NO active booking, hero shows "Bereit für heute" + next booking time. (c) **4-stat strip** (`#FFF4E8` cream tiles, coral eyebrow + Anton tabular numerics): `Heute` (booking count) · `CHF` (today's revenue) · `Walk-in` (current queue count) · `Inbox` (unread messages). (d) **Up next strip** (`#FAF7F3` neutral bg): eyebrow `Als nächstes` + 2-3 rows showing time (tabular per Q43) + client name + service + relative offset (`in 1h`, `2.5h`). (e) **Today activity** (cream bg): bullet list of new bookings / reviews / cancellations from `ActivityFeed`. **Desktop screen (`≥768px`) — Calendar week-grid + sticky stats-strip:** (a) **Sticky 1-row header strip** (`DashboardHeaderStrip` ~120L NEW) — pinned at top, `#FFF4E8` cream bg + `1px solid #F3D8B8` bottom border. Left: gradient "Now" pill (same coral-amber gradient as mobile hero, compressed: eyebrow `Jetzt` + Anton `<client> · <time>`). Right: 4 stat pills (`Heute` count, `CHF` revenue, `Walk-in`, `★` rating — each white bg, coral eyebrow + Anton tabular). (b) **Calendar week-grid** = existing `/dashboard/calendar` route content rendered inline (Mon-Fri 5-day default; Mon-Sun 7-day toggle). Color-coded blocks per booking status: coral `#E8624A` (confirmed), amber `#F3A864` (pending), green `#16A34A` (walk-in), ink `#1A1108` with amber left-border (currently-active "NOW" booking). (c) Today's column highlighted with cream `#FFF4E8` header. **Persistent shell — grouped sidebar (desktop) / slide-from-left drawer (mobile):** **Group anatomy** (top→bottom): (1) **Heute** — single highlighted row, gradient `linear-gradient(135deg,#E8624A 0%,#F3A864 100%)` bg, white text, font-weight 700. The dashboard "home." (2) **Plan** — Kalender · Buchungen · Mitarbeiter · Schließzeiten. (3) **Kunden** — Kunden (count chip, e.g. `128`) · Segmente · Reviews (amber count chip if pending replies) · Nachrichten (coral count chip if unread). (4) **Geschäft** — Services · Gallery · Promos · Last-Minute · Geschenkkarten · Pakete. (5) **Analyse** — Übersicht · Umsatz · SolenScore · Forecast. (6) **{Category}-Tools** — INJECTED based on salon `categories[]`. Coiffeur = Formulas + Beratungen + Color-Zyklus; Barber = Fade-Blueprints + Leaderboard + Walk-in-Analytics; Nails = NailDesigner + ColourLibrary + ToolHygiene; Makeup = Face-Charts + Kit + Bridal-Planner; Spa = Rooms + Wellness-Journal + Treatment-Outcomes; Waxing = Sensitivity-Log + Rebook-Alerts + Zone-Revenue. **Multi-category salons** stack all relevant tool groups. Group label color = coral `#C95A3A` (NOT ink) so category tools telegraph "this is your specialty section". (7) **Einstellungen** — at the bottom border, quiet text-button. **Sidebar anatomy (desktop):** ~160px wide, `#FAF7F3` cream bg, `1px solid var(--b)` right border. Brand block at top (Anton "SOLEN" wordmark + Figtree salon-name eyebrow). Group labels are 6-7px Figtree 700 `.18em` tracking uppercase coral-or-ink3. Nav rows are 8-9px Figtree, 3-4px vertical padding, hover bg `rgba(232,98,74,.06)`, active row gets coral-amber gradient bg + white text + 700 weight. Count chips ride right (5-7px Figtree 700 tabular, coral or amber bg per state). Collapsed-sidebar variant (~52px wide, icons only) toggles via top-left chevron — `localStorage.dashboard.sidebarCollapsed` persists. **Drawer anatomy (mobile):** slide-from-left, 80% viewport width, `#fff` bg with backdrop `rgba(0,0,0,0.4)`. Header = same coral-amber gradient block as Live-Activity hero (salon name + owner name + role/category eyebrow). Same group rows below. Drawer triggered by hamburger in header bar. Tap-outside or swipe-left to close. **Routing:** every nav row routes to its existing dashboard sub-route — no rewrites, just the chrome wraps existing pages. Today row routes to `/dashboard` (the new viewport-split landing). **Existing components reused:** `DashboardLayout.tsx` (replaced — gets the sidebar shell), `StatCard.tsx` (count-up animation reused inside Live card + stats strip), `ActivityFeed.tsx` (reused inside Live card "Today activity" section), all 37 dashboard components stay, all 6 category sub-folders untouched, all 20 sub-routes (`/dashboard/calendar`, `/bookings`, `/clients`, etc.) keep their pages. **NEW components:** `TodayLiveCard.tsx` (~250L mobile-only), `DashboardHeaderStrip.tsx` (~120L desktop sticky), `DashboardSidebar.tsx` (~200L desktop shell with category-tools injection), `DashboardDrawer.tsx` (~150L mobile drawer wraps Sidebar groups), `useDashboardLiveState.ts` (~80L SWR + websocket hook for now/next/walk-in/inbox state). **Anti-patterns banned:** single "responsive" layout that tries to be both mobile and desktop default (they're different jobs — split them) · dashboard homepage that's only stats with no calendar/today data (current state — retires) · category-tools mixed into general nav groups (Formulas in `Geschäft` would clutter the nav for non-coiffeur salons — must be its own injected group) · sidebar without group labels (37 components ungrouped = 37-row spaghetti) · count chips that show on items without state (a `Kalender` count chip showing total bookings = noise; only show counts on items with actionable state — unread messages, pending reviews, customer total context) · `Today` row that doesn't visually telegraph it's the home (must use gradient bg as the only colored row in nav) · sidebar that disappears on scroll (must be sticky, full-height) · mobile drawer triggered by anything other than hamburger (no swipe-from-edge, no tap-on-name — hamburger only per Q58 nav rule) · Live-Activity card on desktop default (the desktop user wants the calendar as their primary surface; the gradient "now" pill in the sticky strip is enough). **Verification:** Q61 in-flight section in `public/solen-coral.html#q61` shows: (a) 4 default-screen variants A/B/C/D with pros/cons, (b) E viewport-split hybrid (recommended) with mobile + desktop side-by-side mockups, (c) sidebar shell with grouped nav including category-tools injection (Coiffeur example) + mobile drawer mockup, (d) why-E-wins recommendation panel with 6 bullet points. |
| 2026-05-02 | **Q62: Dashboard visual differentiation = D — SAME tokens as consumer (white bg + coral `#E8624A` + amber `#F3A864` + Anton + Figtree). NO sub-palette. NO inverted/dark mode (stays retired). One product, two registers — density does the differentiation work, not colors.** | **Token contract (identical to consumer):** background `#fff`, surface-sunken `#FAF7F3`, brand coral `#E8624A`, amber `#F3A864`, ink `#1A1209`, ink-2 `#56463E`, ink-3 `#9F8A7E`, border `#EFE7DD`, status green `#16A34A` / amber-warn `#F3A864` / red-error existing token. Display font Anton, body Figtree. Q48 signature lockup (eyebrow + Anton headline) used identically on both sides. **Only structural divergence:** **sidebar bg = cream `#FAF7F3`** (vs white on consumer). This is the single visible signal that tells the owner "you're in the working tool" without changing the palette. Sidebar group labels = coral `#C95A3A` for category-specific tool groups (per Q61 lock), ink-3 for general groups. Right-content area stays white. **Pro signals via density + structure (NOT color):** keyboard shortcuts surface (`⌘K` palette via existing `CommandPalette.tsx`), tabular numerics on all numeric columns (per Q43, applied dense by default in working surfaces), status pictograms (✓/⏳/❌) instead of text labels in tables, gradient-coral "Now" pill in sticky header (Q61) reuses consumer Q58 grammar so the owner sees their own design language. **Anti-patterns banned:** sub-palette / different brand color for B2B (drift, two products to maintain) · dark mode default (retired list — never reintroduce) · ink-shifted accent colors that don't appear on consumer side · separate font families for B2B · "DASHBOARD" branding chrome that breaks Solen wordmark · cream `#FAF7F3` bg used on the right content area (must stay white — only sidebar gets cream). **Migration:** existing `DashboardLayout.tsx` already uses `s-bg-sunken` + `s-coral` + `s-ink` tokens; no CSS-variable rename needed. Audit `components/dashboard/**/*.tsx` for any hardcoded ink/coral hex that should use the canonical token; flag in drift report (per `_audits/CURRENT_STATE.md` follow-up). **Verification:** Q62-Q63 batched in-flight section in `public/solen-coral.html#q62-q63` shows 4 combinations (A same-spacious / B sub-palette / C uniform-dense / D contextual recommended) + token-contract panel + why-D-wins rationale. |
| 2026-05-02 | **Q63: Dashboard density = D — CONTEXTUAL density per surface, NOT uniform. Dense in working surfaces (calendar, bookings, customers, analytics, inbox, walk-in queue). Comfortable (consumer-matching) in editor surfaces (settings, gallery editor, salon-page editor, package builder, promo configs, gift-card design).** | **Dense mode spec:** row height 28-32px, vertical padding 4-6px, horizontal padding 8-10px, font 9-11px Figtree, line-height 1.3, gap-between-rows 0 (border-bottom only), tabular numerics ON for all numeric columns (per Q43), status pictograms (✓ confirmed / ⏳ pending / ❌ cancelled / 🔵 walk-in) replace text labels, hover bg `rgba(232,98,74,0.04)`, active row bg `#FFF4E8`, NO row-level shadows. Headers compressed: 5-6px Figtree 700 `.18em` tracking uppercase coral-eyebrow. **Comfortable mode spec:** row height 48-56px, vertical padding 12-16px, horizontal padding 14-18px, font 13-15px Figtree, line-height 1.5, gap-between-rows 6-8px (per Q32 spacing), proportional-or-tabular numerics per Q43 rules, full text labels (no pictogram-only), card-shadow per Q30 in-flow tier. Headers full-size: Anton 16-22px + coral eyebrow. Identical to consumer surfaces. **Surface mapping — DENSE:** `/dashboard/calendar` (week-grid event blocks), `/dashboard/bookings` (table list), `/dashboard/clients` (customer table), `/dashboard/analytics` + `/dashboard/revenue` (data tables, charts retain own sizing), `/dashboard/messages` (conversation list), `/dashboard/reviews` (review queue rows), all `/dashboard/{category}/*` data lists (Walk-in queue, Leaderboard, Sensitivity log, Wellness journal entries, Color-cycle metrics rows, etc.). **Surface mapping — COMFORTABLE:** `/dashboard/settings/**`, `/dashboard/gallery` (image grid editor), `/dashboard/editor` (salon-page editor), `/dashboard/services` (service builder forms), `/dashboard/discovery-posts` (post composer), `/dashboard/segments` (segment builder), category-specific editors (`FaceChartBuilder`, `FadeBlueprint` editor, `BridalPlanner`, `KitInventory` add/edit, `RoomManager`, `PackageManager`, `OffPeakManager`, `PromoManager`, `GiftCardManager`, `LastMinuteManager`, `SalonAboutEditor`). **Implementation:** add `density?: "dense" | "comfortable"` prop to all reusable list/table/grid primitives (`StaffTable`, `BookingList`, custom data tables in category folders); default = `"comfortable"` (so consumer-side surfaces and any new editor surface get the safe register); dense set explicitly per-route in working surfaces. Tailwind: `data-[density=dense]:py-1 data-[density=dense]:text-[10px] data-[density=dense]:px-2` etc., or per-component conditional classes. **Anti-patterns banned:** third "in-between" density (e.g. medium 36-40px rows) — pick one per surface; dense mode in editor surfaces (cramming form inputs sucks, kills proofreading); comfortable mode in working surfaces (pro can't see enough rows, the whole point of the dashboard fails); mixing dense and comfortable rows in the SAME table (no "first row dense, second row comfortable" hybrid); using density to mean "compact card grid" — density refers to ROW height + padding, not card count per row (that's grid-density, separate concern). **Existing component audit:** `StatCard` already has 2 size variants — keep. `ScheduleGrid`, `WalkinAnalytics`, `BarberLeaderboard`, etc. need a density prop add. `CommandPalette.tsx` (already exists) IS dense by default — no change. Editor components were already spacious — no change. **Verification:** Q62-Q63 batched in-flight section in `public/solen-coral.html#q62-q63` shows 4 combinations + dense vs comfortable spec table + per-surface mapping list + density="dense"|"comfortable" component prop pattern. |
| 2026-05-03 | **Q64: GREEN PIVOT — primary brand color flips from coral `#E8624A` to forest green `#1B4D1B`. Amber `#F3A864` stays as warm secondary accent. All semantic tokens (love-red heart `#FF4A6B`, success-status green `#16A34A`, error red, warm grey, warm ink, warm-ink shadows) STAY UNCHANGED per SOLEN_UI #5b. Single-spot exception: hero accent line ("DIREKT GEBUCHT.") stays amber, NOT green.** | **Trigger:** the 2026-04-30 in-flight pivot inside `public/solen-coral.html:411-450` was ratified by user 2026-05-03 after multi-week iteration. The reference banner ("● SOLEN NEW SYSTEM IN FLIGHT — GREEN + AMBER + ANTON + FIGTREE") is now locked. **Token map (locked):** `--coral` `#E8624A` → `#1B4D1B` (forest green) · `--coral-h` (hover) → `#0F3010` (deep green) · `--coral-s` (subtle/tint bg) `#FAECE7` → `#E8EFE4` (light green tint) · `--coral-t` (deep text variant) `#C95A3A` → `#0F3010` (deep green text). Tailwind `s-coral.{DEFAULT,hover,subtle,text,button,button-hover}` all flip to corresponding green hexes. Amber tokens unchanged. **Q-locks superseded by Q64 (specific clauses, not whole locks):** **Q23** "no green in UI accents" inverted — green IS the brand primary; the prior "coral + amber as the ONLY accent palette" rule reads as "**green** + amber as the ONLY brand-accent palette" (semantic green for status `#16A34A` was always allowed and stays distinct from brand green `#1B4D1B`). **Q47** focus ring color flips from `#E8624A` to `#1B4D1B` — `*:focus-visible { outline: 2px solid #1B4D1B; outline-offset: 2px }`. Contrast: `#1B4D1B` on white = **9.89:1** (passes AA body 4.5:1 easily, far higher than coral's 3.35:1). **Q45** banned-pair #3 (white-on-coral 3.35:1) NO LONGER applies — white-on-brand-green is **9.89:1**, AA body passes for any text size. The other two banned pairs (white-on-amber, amber-text-on-white) are unchanged. **Q62** dashboard token contract — `coral #E8624A` → `green #1B4D1B`; the "SAME tokens as consumer" discipline holds (consumer + B2B both flip together). **What does NOT flip (per SOLEN_UI #5b semantic-token discipline, line 305 explicitly anticipates this pivot):** (1) **love-red heart save** stays literal `#FF4A6B` — it's the love-save semantic, NOT a brand token reference. Heart icons must continue to use the inline-style override pattern, never `text-s-coral` / `var(--coral)`. (2) **success-status green** stays `#16A34A` — distinct from brand green `#1B4D1B`. Used for "Heute frei" availability chips, success toasts, walk-in queue confirmations. The two greens differ visually (brand is dark forest, status is brighter mid-green) — never collapse them. (3) **amber `#F3A864`** stays — secondary accent for ratings, eyebrow on dark register, "Ohne Anrufen" promise pill, hero accent line "DIREKT GEBUCHT." (4) **warm-ink** `#1A1209` body text + **warm-grey** `#9F8A7E` neutral + **warm-ink-tinted shadows** `rgba(26,18,9,0.x)` all unchanged — the warm tonal family is ind subordinate of brand color choice. (5) **warm-cream surfaces** `#FFF4E8` / `#FAF7F3` unchanged. **Hero accent override (Q48 + S1 hero refit):** the second line of the homepage hero ("DIREKT GEBUCHT.") stays AMBER not green, mirroring the reference's `.hero-h1 .coral { color: var(--amber) !important; }` override at line 451. Implementation: `<SignatureLockup accentLine>` continues to render the second line in amber `#F3A864` regardless of brand pivot — this is hard-coded in the component, NOT derived from `s-coral`. **Implementation sequence (already started 2026-05-03):** (1) Append Q64 to SOLEN_DESIGN.md ✓. (2) Update `_rules/SOLEN_UI.md` — replace "coral" brand-color references in rule 10, the output gate examples (lines 174-182), and the color-theory section (lines 223-227). Keep rule #5b unchanged (already pivot-aware). (3) Flip `tailwind.config.js` `s-coral.*` tokens to green hexes. (4) Sweep `app/globals.css` for hardcoded `#E8624A` (focus ring rules, glass-search border, button overrides). (5) Verify token-class consumers auto-flipped via dev-server reload. (6) Run hardcoded-coral-hex sweep on components: `grep -rn '#E8624A\|#C95A3A\|#D4574A\|#FAECE7' components/ app/` — apply Guardrail A per L8 (do NOT sweep `#FF4A6B` love-red, `#16A34A` success green, `#F3A864` amber). (7) Visual diff against `public/solen-coral.html` per Guardrail C. **Anti-patterns banned post-pivot:** (a) heart icons using brand-green token (must stay love-red `#FF4A6B`); (b) success toasts using brand green `#1B4D1B` instead of status green `#16A34A`; (c) amber suddenly used for CTAs in place of brand green (amber stays secondary); (d) any new component referencing the OLD coral hex `#E8624A` (will read as drift); (e) "purist" green-on-green (brand green CTA on green-tint bg) — same hue lacks contrast hierarchy, use white-on-green for CTAs and ink-on-white for body. **Verification:** Live preview at `localhost:3000/de` should match `localhost:3000/solen-coral.html` brand-color register. Test cases: hero "BEAUTY." stays ink + "DIREKT GEBUCHT." stays amber; hero search-card "Suchen" button = green; focus-visible outline on all interactive = green; SalonCard heart save = love-red `#FF4A6B`; salon-detail "Heute frei" availability chip = sage tint `#EBF5EE` bg + sage text `#2E5E3A` (status, not brand); booking-success toast = `#16A34A` (status). |
