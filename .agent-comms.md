# Agent Communication Log

> **Every AI agent MUST read the latest entries before starting work, and post their own entry before editing files.**
> Append new entries at the TOP (newest first). Never delete old entries — they serve as audit trail.

---

### [2026-03-16 09:15] bug-agent — DONE
**Fixed**: "Salons konnten nicht geladen werden" bug in `index.html:loadStores()`.
- Replaced `stores` query with `salons` query using `is_active=true`
- Added column-normalisation shim: `categories→cat`, `latitude→lat`, `longitude→lng`, `average_rating→avg_rating`, `cover_photo_url→image_url`, `instagram_url→instagram`, `quartier→neighbourhood`
- Services: added `name` alias from `name_de||name_en` for render compatibility
- Bookings: removed non-existent `store_id` column from select
- **Side effects**: Other agents using `allStores` or `allServices` globals will now receive correctly mapped objects. No breaking changes to downstream render code.

### [2026-03-16 09:10] bug-agent
**Starting**: Fixing "Salons konnten nicht geladen werden" homepage error.
- **Root cause**: Migration 013 dropped the `stores` table; `loadStores()` still queries it first, gets an error, and aborts before ever reaching the `salons` query.
- **Files**: `index.html` (locked)
- **Fix**: Rewrite `loadStores()` to query `salons` directly with new schema, add column-name mapping (categories→cat, latitude→lat, etc.), fix service name mapping (name_de→name), fix bookings query.

---

### [2026-03-16 09:06] system
**Setup**: Multi-agent coordination system initialized.
- Created `CLAUDE.md` (master instructions)
- Created `.agent-lock.json` (file locks)
- Created `.agent-comms.md` (this file)
- Created `_tasks/` folder for task tracking & archival

All agents: read `CLAUDE.md` fully before starting any work.

---
