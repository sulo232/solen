# Agent Communication Log

> **Every AI agent MUST read the latest entries before starting work, and post their own entry before editing files.**
> Append new entries at the TOP (newest first). Never delete old entries — they serve as audit trail.

---

### [2026-03-16 15:00] feature-agent (Dev 3) — SALON DASHBOARD COMPLETE
**Branch**: `feature/salon-dashboard`
**Completed**: All 11 phases — DashboardLayout, onboarding wizard (6 steps), overview, booking management, calendar, messaging (ChatWindow perspective='salon'), staff, services, analytics (recharts), settings (5 tabs), verification UI.
**Missing APIs** (Dev 1 must build before dashboard works E2E):
- `/api/staff` (GET/POST/PATCH/DELETE) — not in codebase
- `/api/services` (GET/POST/PATCH/DELETE) — not in codebase
- `/api/slots/{id}` DELETE + `/api/slots/bulk` POST — not in codebase
- `/api/analytics/salon/{id}` — not in codebase
- `PATCH /api/salons/{id}` — only GET by slug exists
- `/api/profile` must return `salon_id`, `salon_name`, `salon_categories` fields for dashboard to work
**Files touched**: `components/dashboard/DashboardLayout.tsx`, all `app/[locale]/dashboard/` pages, `app/[locale]/onboarding/salon/page.tsx`, `package.json` (added recharts)
**Side effects**: Added `recharts` dependency. No customer pages touched. No `index.html` touched.

---

### [2026-03-16 14:30] feature-agent (Dev 2) — ROADMAP COMPLETE
**Branch**: `feature/customer-frontend`
**Completed**: Phase 7 Stornieren cancel button (modal, 24h guard, tooltip, rebook shortcut) + Phase 8 TutorialTour (driver.js, 4-step, localStorage flag). Dev 2 roadmap fully done.
**Files**: `app/[locale]/account/page.tsx`, `components/TutorialTour.tsx`, `components/HomePage.tsx`, `components/layout/Header.tsx`, `components/index.ts`

---

### [2026-03-16 12:30] feature-agent — PHASE 2 DONE
**Completed**:
- `PriceSlider` → real `@radix-ui/react-slider` dual-thumb, teal track, 300ms debounce → URL params
- `MapView` → real Mapbox GL JS v3, teal/coral pins, bounds fit, pan+popup on select
- `components/HomePage.tsx` (new) — weather banner, search+autocomplete (debounced 300ms), Quartiere horiz scroll, Services horiz scroll, Last-Minute teaser, Recommendations, Social Proof count-up + review carousel, Nudge banner
- `app/[locale]/page.tsx` — **iframe replaced** with `<HomePage />`
⚠️ `NEXT_PUBLIC_MAPBOX_TOKEN` must be set in `.env.local` for MapView.
- Phases done: 1, 2, 3. Next: Phase 4 (Salon Profile + BookingCalendar).

---

### [2026-03-16 09:15] bug-agent — DONE
**Fixed**: "Salons konnten nicht geladen werden" bug in `index.html:loadStores()`.
- Replaced `stores` query with `salons` query using `is_active=true`
- Added column-normalisation shim: `categories→cat`, `latitude→lat`, `longitude→lng`, `average_rating→avg_rating`, `cover_photo_url→image_url`, `instagram_url→instagram`, `quartier→neighbourhood`
- Services: added `name` alias from `name_de||name_en` for render compatibility
- Bookings: removed non-existent `store_id` column from select
- **Side effects**: Other agents using `allStores` or `allServices` globals will now receive correctly mapped objects. No breaking changes to downstream render code.

### [2026-03-16 09:10] bug-agent
**Starting**: Fixing "Salons konnten nicht geladen werden" homepage error.
- **Root cause**: Migration 013 dropped the `stores` table; `loadStores()` still queries it first, gets an error, and aborts before ever reaching the `salons` query.
- **Files**: `index.html` (locked)
- **Fix**: Rewrite `loadStores()` to query `salons` directly with new schema, add column-name mapping (categories→cat, latitude→lat, etc.), fix service name mapping (name_de→name), fix bookings query.

---

### [2026-03-16 09:06] system
**Setup**: Multi-agent coordination system initialized.
- Created `CLAUDE.md` (master instructions)
- Created `.agent-lock.json` (file locks)
- Created `.agent-comms.md` (this file)
- Created `_tasks/` folder for task tracking & archival

All agents: read `CLAUDE.md` fully before starting any work.

---
