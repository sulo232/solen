# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Sentry Config File
.env.sentry-build-plugin

# MCP server config — contains secret API keys (Obsidian Local REST API bearer
# token, etc.). NEVER commit. Each developer creates their own .mcp.json from
# .mcp.json.example if needed.
.mcp.json

# Worktrees
.worktrees/

# Next.js
.next/
out/
build/
tsconfig.tsbuildinfo

# Vercel
.vercel/

# Claude / Agent State — top-level ignore, but track the persistent project
# configuration (settings, hooks, custom subagents, custom slash commands) so
# the design-token sweep guard + verifier infrastructure survive sessions/clones.
.claude/*
!.claude/settings.json
!.claude/launch.json
!.claude/hooks/
!.claude/hooks/**
!.claude/agents/
!.claude/agents/**
!.claude/commands/
!.claude/commands/**
.claude/sweep-approved.flag
.agent-*
.npm-cache/
.npm-local-cache/
supabase/.temp/

# Environment
.env
.env.*
!.env.example
.env.*.local
.env.development

_archive/
node_modules_old/
.vercel
docs/design-specs/CLAUDE_CODE_PROMPT.md

# Playwright — keep baselines, ignore artifacts
e2e/visual/test-results/
e2e/visual/report/
playwright-report/
blob-report/
.playwright-mcp/

# Claude skills runtime cache (skills tracked via skills-lock.json,
# but the unpacked source under .agents/skills/ is a local cache).
.agents/

# Screenshot artifacts at repo root (Playwright/screenshot tool dumps).
# Only the root-level pattern — public/*.png and other tracked PNGs stay tracked.
/*.png
